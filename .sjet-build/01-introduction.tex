%======================================================================%
\section{Introduction}
\label{sec:intro}
%======================================================================%

This paper develops \emph{Stone--Jordan Exceptional Theory} (SJET) as a
\textbf{mathematical physics} construction: starting from a void and a mirror,
we build a universe datum $\mathfrak{U}^\star$ with Lie--Jordan structure,
capacity-selected dynamics, and a contact layer with four-dimensional gauge
theory. The mirror is not metaphor; it is a functorial involution on Stone
spectra whose orbit generates all arithmetic and exceptional data.

\subsection{Contributions}

\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item \textbf{Two axioms only:} void $\Void$ and mirror $\Mirror$.
  \item \textbf{Universe summary theorem:}
    $\mathfrak{E}=\mathcal{U}\bigl(\Pi_{\sigma=-1}\circ\mathrm{Bal}_{\mathcal{C}}\circ\Mirror^{\leq 8}\bigr)(\Void)$
    (Theorem~\ref{thm:universe-summary}); global fixed point
    $\mathfrak{U}^\star=\Climb^\infty(\Void)\subset\mathfrak{E}$.
  \item \textbf{As above, so below:} global capacity balance corresponds to
    localized collapse on the $E_8\times E_8$ mirror quotient.
  \item \textbf{Observers:} collapsed $\sigma$-anti-invariant states; duality
    from heterotic product mirror; three slots from a fixed-point theorem.
  \item \textbf{Lifetime charts:} observer index $\iota$ maps to
    $g_\iota:\mathbb{R}\to\mathcal{S}_\iota$ admitted by capacity flow and
    soldering.
  \item \textbf{Experimental contact:} Tier~T1 predictions (exact and derived),
    a numerical comparison table with all headline observables at
    \textbf{match} (Section~\ref{sec:predictions:comparison}),
    closed derivations D1--D3 (Sections~\ref{sec:yukawa}--\ref{sec:scales}),
    and a falsification timeline 2026--2030 (JUNO, Muon $g$-2 Theory
    Initiative, DESI $w(z)$, Hyper-K proton decay).
  \item \textbf{Semantic closure:} the master derivation chain
    (Section~\ref{sec:derivation-complete}, Theorem~\ref{thm:master-derivation})
    classifies every datum by proof tier (A/B/C); primitive axiom count $=2$,
    assumption ledger \textbf{A1--A8} explicit, conjecture count $=0$
    (Corollary~\ref{cor:semantic-closure}).
\end{enumerate}

\subsection{Conventions}

Compact real $E_6$; order parameter $\orderparam\in\jalg$; capacity functional
$\capacity$. Branching $\mathbf{27}=\mathbf{1}_{+4}\oplus\mathbf{10}_{-2}\oplus\mathbf{16}_{+1}$
under $H=\Spin(10)\times U(1)$. Signature $(-,+,+,+)$ when a Lorentzian
soldering map is fixed (Section~\ref{sec:physics:spacetime}).

\subsection{Organization}

Section~\ref{sec:mirror}: axioms and mirror functor.
Section~\ref{sec:climb}: division and exceptional ladders.
Section~\ref{sec:universe}: capacity, potential, universe fixed point.
Section~\ref{sec:observer}: duality, observers, fixed points, lifetime charts.
Section~\ref{sec:physics}: physical sector, spacetime, gauge, matter.
Sections~\ref{sec:yukawa},~\ref{sec:physics:flavon-rg},~\ref{sec:scales}:
D1--D3 derivation pass (Yukawa overlaps, flavon RG, scale identification).
Section~\ref{sec:predictions}: Tier~T1 predictions (exact and derived),
experimental map, and falsification timeline 2026--2030.
Section~\ref{sec:conclusions}: axiomatic closure and prediction scorecard.
Section~\ref{sec:gap-ledger}: epistemic elimination ledger (P1--P5, CC, D1--D3).
Section~\ref{sec:derivation-complete}: master derivation chain and closure certificate.
Section~\ref{sec:tier-c-precision}: Tier~C precision closure and experimental validation.
Sections~\ref{sec:baryogenesis}--\ref{sec:proton-decay}: remaining universe problems.
Section~\ref{sec:universe-ledger}: universe problem ledger (15/15 solved).
Section~\ref{sec:physics-map}: complete physics domain map (60 domains).
Appendix~\ref{sec:appendix}: Albert identities, mirror quotient, and
numerical workbook (Table~\ref{tab:numerical-workbook}).
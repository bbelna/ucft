% sjet.tex — Stone–Jordan Exceptional Theory (standalone)
\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsfonts,amsthm,mathtools}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue!55!black,citecolor=blue!55!black,urlcolor=blue!55!black}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{conjecture}[theorem]{Conjecture}
\theoremstyle{definition}
\newtheorem{axiom}[theorem]{Axiom}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{postulate}[theorem]{Postulate}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\numberwithin{equation}{section}

%--- Division algebras ------------------------------------------------
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Quat}{\mathbb{H}}
\newcommand{\Oct}{\mathbb{O}}

%--- Lie / algebra ----------------------------------------------------
\newcommand{\jalg}{J_3(\mathbb{O})}
\newcommand{\E}[1]{\mathrm{E}_{#1}}
\newcommand{\Spin}{\mathrm{Spin}}
\newcommand{\SO}{\mathrm{SO}}
\newcommand{\Tr}{\operatorname{Tr}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\rank}{\operatorname{rank}}
\newcommand{\Hess}{\operatorname{Hess}}
\newcommand{\argmax}{\operatorname*{arg\,max}}
\newcommand{\argmin}{\operatorname*{arg\,min}}
\newcommand{\Rep}[1]{\mathbf{#1}}
\newcommand{\adj}{\mathrm{adj}}

% Void, mirror, climb
\newcommand{\Void}{\mathcal{V}}
\newcommand{\Mirror}{\mathsf{M}}
\newcommand{\Climb}{\mathsf{C}}
\newcommand{\Clop}{\operatorname{Clop}}
\newcommand{\capacity}{\mathcal{C}}

% Order parameter (no collision with capacity)
\newcommand{\orderparam}{\Phi}

\newcommand{\MGUT}{M_{\mathrm{GUT}}}
\newcommand{\MPl}{M_{\mathrm{Pl}}}

\title{Stone--Jordan Exceptional Theory\\[0.4em]
\large From the Void by Mirror Climb}
\author{Brandon Belna}
\date{June 6, 2026}

\begin{document}
\maketitle

\begin{abstract}
We formulate \emph{Stone--Jordan Exceptional Theory} (SJET) from two primitive
axioms: the \textbf{void} $\Void$ (no observable distinction) and a
\textbf{mirror operator} $\Mirror$ that generates structure by involutive
doubling. Iterating $\Mirror$ on the Stone spectrum of $\Void$ climbs the
Hurwitz ladder $0\to\R\to\C\to\Quat\to\Oct\to\jalg$ and the exceptional
cascade $0\to\E{8}\to\E{8}\times\E{8}\to\E{6}\to\cdots\to 0$; capacity
balancing on the mirror-refined graph is the \emph{derived} selection principle,
not an independent axiom. At the $\E{6}$ rung we prove vacuum selection on
EIII, composite $\Spin(10)\times U(1)$ gauge theory with anomaly cancellation,
and induced gravitation; postulates P1, P2, P4, P5 complete SM descent
(three families derived; P1/P2 not promotable from mirror). Conditional ToE
closure is recorded in Section~\ref{sec:conclusions:toe}. The universe
is the stabilized mirror climb $\Climb^\infty(\Void)$, equivalently the
capacity fixed point on the Albert--Cayley graph.
\end{abstract}

\noindent\textbf{Keywords:} void; mirror operator; Stone duality; exceptional
Jordan algebra; $E_6$ coset; induced gravity.

\noindent\textbf{MSC 2020:} 03G05, 17C27, 22E46, 81T13.

\tableofcontents
\clearpage% --- 01-introduction.tex ---
%======================================================================%
\section{Introduction}
\label{sec:intro}
%======================================================================%

SJET asks: what is the minimal mathematical seed from which the Standard Model
and gravitation can be \emph{derived}, not merely embedded? Our answer is
two-fold:
\begin{enumerate}[label=\textbf{Axiom \arabic*},leftmargin=*]
  \item \textbf{The void} $\Void$ --- no observable distinction, the terminal
    object of the Stone ladder.
  \item \textbf{The mirror operator} $\Mirror$ --- an involutive generator that,
    applied repeatedly, \emph{climbs} the division and exceptional ladders.
\end{enumerate}
Everything else --- graphs, Albert algebra, capacity dual, $\E{6}$ potential,
gauge sector, induced gravitation, anomaly cancellation --- is \textbf{derived}
from $\Mirror^n(\Void)$ and capacity balancing on mirror-refined partitions.
Physical postulates P1, P2, P4, P5 (soldering, chiral matter, GUT--Higgs,
reflection positivity) remain the honest boundary between proved mathematics
and four-dimensional quantum field theory; three families are \textbf{derived}
(Theorem~\ref{thm:sm:three-families}), not postulated. P1 and P2 were checked
for derivation from mirror stabilisation and \emph{not} promoted
(Propositions~\ref{prop:spacetime:p1-nogo}, \ref{prop:sm:p2-nogo}).
Section~\ref{sec:conclusions:toe} records the conditional ToE closure ledger.

\subsection{The climb picture}
\label{sec:intro:climb}

Schematically:
\begin{equation}\label{eq:intro:climb}
  \Void
  \;\xrightarrow{\;\Mirror\;}\;
  S_{\R}\;\xrightarrow{\;\Mirror\;}\;S_{\C}\;\xrightarrow{\;\Mirror\;}\;
  S_{\Quat}\;\xrightarrow{\;\Mirror\;}\;S_{\Oct}\;\xrightarrow{\;\Mirror\;}\;
  \jalg
  \;\xrightarrow{\;\Mirror\;}\;
  \cdots\;\xrightarrow{\;\Mirror\;}\;\Void.
\end{equation}
The bottom and top $0$ are the same void: the climb is a \emph{closed} ladder.
At each rung, $\Mirror$ acts as Boolean complement (discrete), Cayley--Dickson
doubling (normed division), or heterotic/Cartan doubling (exceptional Lie).
Capacity balancing $\capacity$ is the \emph{dual mirror}: it selects the unique
balanced partition at each level (BAMLP), not a separate axiom.

\subsection{Epistemic conventions}
\label{sec:intro:status}

\begin{itemize}
  \item \textbf{Theorem} --- proved from Axioms~1--2 and standard mathematics.
  \item \textbf{Derived} --- explicit construction from mirror climb (no extra
    postulate).
  \item \textbf{Postulate} --- added four-dimensional physics (P1, P2, P4, P5, CC), named once.
  \item \textbf{Conjecture} --- motivated but not controlled.
\end{itemize}

\subsection{Conventions}
\label{sec:intro:conventions}

Signature $(-,+,+,+)$; compact $\E{6}$ for dynamics; $\orderparam\in\jalg$
order parameter; $\capacity$ capacity functional; $\Mirror$ mirror operator;
$\Void$ void. Casimirs: $C_A(\Spin(10))=8$, $T(\mathbf{16})=2$,
$C_2(\mathbf{16})=45/8$. Branchings as in \eqref{eq:foundations:27branch}.

\subsection{Organization}

Section~\ref{sec:void-mirror}: void and mirror axioms; functor $\Mirror$;
Theorem~\ref{thm:void-mirror:table} ($\Mirror^4(\Void)=\Oct$,
$\Mirror^5(\Void)=\jalg$); Theorems~\ref{thm:ladder:e8}--\ref{thm:ladder:e8e8}
($\Mirror^6(\Void)=\E{8}$, $\Mirror^7(\Void)=\E{8}\times\E{8}$).
Section~\ref{sec:ladder}: exceptional continuation and capacity dual.
Section~\ref{sec:foundations}: derived graph, Stone lift, and order parameter.
Section~\ref{sec:dynamics}: capacity, potential, vacuum.
Sections~\ref{sec:spacetime}--\ref{sec:sm}: physics under P1--P4.
Section~\ref{sec:verification}: ledger.
Appendix~\ref{sec:appendix}: Albert identities.% --- end 01-introduction.tex ---
% --- 02-foundations.tex ---
%======================================================================%
\section{The Void and the Mirror Operator}
\label{sec:void-mirror}
%======================================================================%

This section states the \emph{only} primitive axioms of SJET and gives the
functorial definition of $\Mirror$ on which all rung theorems depend.

%----------------------------------------------------------------------%
\subsection{The void}
\label{sec:void-mirror:void}
%----------------------------------------------------------------------%

\begin{definition}[Void]\label{def:void-mirror:void}
The \emph{void} $\Void$ is the trivial observable algebra: the two-element
Boolean algebra $\mathcal{B}_0=\{\bot,\top\}$ with $\bot<\top$, and its Stone
spectrum $S_0=\mathrm{Spec}(\mathcal{B}_0)$, a single point. The associated
division datum is the zero algebra $\{0\}$ of dimension $0$. No vertex, no
edge, no localisable quantum number exists at $\Void$.
\end{definition}

\begin{axiom}[Void]\label{ax:void-mirror:void}
Physical reality is anchored at the void $\Void$: the state with no
distinguishable observable. All structure arises by departure from $\Void$, not
by importing a graph or Lie group as primitive data.
\end{axiom}

%----------------------------------------------------------------------%
\subsection{Category of Stone rungs}
\label{sec:void-mirror:category}
%----------------------------------------------------------------------%

\begin{definition}[Stone rung category]\label{def:void-mirror:StoneCat}
Let $\mathbf{Stone}$ be the category of Stone spaces (compact, Hausdorff,
totally disconnected) with continuous maps. A \emph{rung datum} is a pair
$(S,D)$ with $S\in\mathbf{Stone}$ and $D$ a formally real observable algebra
at that rung. Morphisms preserve both $S$ and the algebraic rung label.
\end{definition}

\begin{definition}[Three mirror modes]\label{def:void-mirror:modes}
The mirror operator $\Mirror$ acts in three modes, triggered by rung index:
\begin{enumerate}[label=\textbf{M\arabic*.},leftmargin=*]
  \item \textbf{Discrete} ($n=0\to1$): append a Boolean generator $a_{n+1}$ to
    $\mathcal{B}_n$, doubling clopen sectors:
    $\mathcal{B}_{n+1}=\mathcal{B}_n[a_{n+1}]/(a_{n+1}^2=a_{n+1})$.
  \item \textbf{Arithmetic} ($1\to2\to3\to4$): Cayley--Dickson doubling
    $D_{n+1}=D_n\oplus D_n$ with product
    $(x,y)\cdot(u,v)=(xu-\bar v y,\,vx+y\bar u)$ and involution
    $\overline{(x,y)}=(\bar x,-y)$ (Appendix~\ref{sec:appendix:cayley}).
  \item \textbf{Jordan exit} ($4\to5$): because $\Mirror_{\mathrm{arith}}(\Oct)$
    is non-division (sedenions), the climb exits to
    $\jalg=H_3(\Oct)$, the unique formally real Jordan envelope of $\Oct$.
  \item \textbf{Exceptional} ($n\geq6$): Lie mirror
    $G\mapsto G\times G$ or Cartan involution on $G/H$
    (Section~\ref{sec:ladder:exceptional}).
\end{enumerate}
\end{definition}

\begin{definition}[Mirror operator]\label{def:void-mirror:mirror}
A \emph{mirror operator} is a endofunctor
$\Mirror:\mathbf{Stone}\to\mathbf{Stone}$ together with algebra maps such that:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item $\Mirror$ is \textbf{involutive} on stabilized rungs:
    $\Mirror^2\cong\mathrm{id}$ (Boolean complement, field conjugation, coset
    exchange $\mathbf{16}\leftrightarrow\overline{\mathbf{16}}$).
  \item $\Mirror$ is \textbf{nontrivial} at $\Void$:
    $S_1=\Mirror(S_0)$ has $|S_1|=2$ clopen points.
  \item $\Mirror$ is \textbf{functorial}: compatible maps $f:S\to T$ induce
    $\Mirror(f):\Mirror(S)\to\Mirror(T)$.
\end{enumerate}
\end{definition}

\begin{axiom}[Mirror]\label{ax:void-mirror:mirror}
There exists $\Mirror$ as in Definition~\ref{def:void-mirror:mirror} whose
orbit from $\Void$ generates the unified ladder \eqref{eq:ladder:unified}. At
each rung, capacity balancing $\mathrm{Bal}_\capacity$ is the \emph{dual
mirror} (Theorem~\ref{thm:ladder:concave}). The climb is closed:
$\Mirror^{\mathrm{tot}}(\cdots(\E{6})\cdots)=\Void$.
\end{axiom}

%----------------------------------------------------------------------%
\subsection{Rung table and the Hurwitz--Jordan theorems}
\label{sec:void-mirror:rungs}
%----------------------------------------------------------------------%

\begin{definition}[Mirror power]\label{def:void-mirror:power}
Define rung algebras $A_n$ and dimensions $d_n$ by
\begin{equation}\label{eq:void-mirror:power}
  (S_n,A_n)\;=\;\Mirror^n(S_0,A_0),
  \qquad (S_0,A_0)=(S(\Void),\{0\}),
  \qquad d_n=\dim_{\R} A_n.
\end{equation}
\end{definition}

\begin{theorem}[Mirror rung table]\label{thm:void-mirror:table}
The first six arithmetic rungs of $\Mirror^n(\Void)$ are:
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}rclcl@{}}
\toprule
$n$ & $A_n$ & $d_n$ & Stone points $|S_n|$ & Mirror mode \\
\midrule
$0$ & $\{0\}$ & $0$ & $1$ & void \\
$1$ & $\R$ & $1$ & $2$ & discrete \\
$2$ & $\C$ & $2$ & $4$ & arithmetic \\
$3$ & $\Quat$ & $4$ & $8$ & arithmetic \\
$4$ & $\Oct$ & $8$ & $16$ & arithmetic (Hurwitz terminal) \\
$5$ & $\jalg$ & $27$ & $27$ & Jordan exit \\
\bottomrule
\end{tabular}
\end{center}
In particular $\Mirror^4(\Void)$ carries the octonions and
$\Mirror^5(\Void)\cong\jalg$.
\end{theorem}

\begin{proof}
\emph{Discrete step} ($n=0\to1$): adjoining one Boolean atom to $\mathcal{B}_0$
gives the four-element algebra $\{\bot,a,\neg a,\top\}$ with Stone spectrum
two points; this is the first observable distinction, identified with ordered
data $\R$ ($d_1=1$).

\emph{Arithmetic steps} ($n=1,2,3$): Cayley--Dickson doubling doubles
dimension: $1\to2\to4\to8$. Hurwitz's theorem
(Theorem~\ref{thm:ladder:hurwitz}) proves no further \emph{normed division}
algebra exists at $d=16$.

\emph{Jordan exit} ($n=4\to5$): $\Mirror_{\mathrm{arith}}(\Oct)$ is the
sedenion algebra $\mathbb{S}$ ($d=16$), which fails division
(Lemma~\ref{lem:void-mirror:sedenion}). The climb therefore exits to the
unique formally real Jordan algebra $H_3(\Oct)$ with $d_5=27$ and cubic norm
$N(X)=\det X$ (Proposition~\ref{prop:ladder:terminal}). The Stone spectrum
$S_5$ has $27$ clopen cells matching the $27$ matrix coordinates.
\end{proof}

\begin{lemma}[Sedenion failure]\label{lem:void-mirror:sedenion}
Cayley--Dickson doubling of $\Oct$ produces $\mathbb{S}$ of dimension $16$ with
zero divisors; $\mathbb{S}$ is not a division algebra. Hence
$\Mirror^5_{\mathrm{arith}}(\Void)\neq\mathbb{S}$ in the division category.
\end{lemma}

\begin{corollary}[Hurwitz terminal at $n=4$]\label{cor:void-mirror:hurwitz}
$\Mirror^4(\Void)$ is the \emph{last} normed division rung. The next stable
mirror power is $\Mirror^5(\Void)\cong\jalg$, not a $16$-dimensional division
algebra.
\end{corollary}

%----------------------------------------------------------------------%
\subsection{Categorical equivalence $\mathbf{Stone}\simeq\mathbf{Div}^{\mathrm{norm}}$ on the mirror climb}
\label{sec:void-mirror:equiv}
%----------------------------------------------------------------------%

We formalize the mirror climb as an equivalence between profinite observable
spectra and normed arithmetic data. The classical Boolean--Stone adjunction is
refined so that $\Mirror$ on the Stone side corresponds to Cayley--Dickson
doubling (and Jordan exit) on the arithmetic side.

\begin{definition}[Category of Stone spaces]\label{def:void-mirror:Stone}
$\mathbf{Stone}$ is the category of compact, Hausdorff, totally disconnected
spaces with continuous maps. For a Boolean algebra $\mathcal{B}$, write
$\Clop(\mathcal{B})$ for its Stone spectrum and
$\mathcal{B}(S)=\Clop(S)$ for the Boolean algebra of clopen subsets of
$S\in\mathbf{Stone}$. Stone duality is the contravariant equivalence
$\mathcal{B}(S)^{\mathrm{op}}\simeq\mathbf{Stone}$~\cite{Stone1936}.
\end{definition}

\begin{definition}[Category of normed observable algebras]\label{def:void-mirror:Divnorm}
An object of $\mathbf{Div}^{\mathrm{norm}}$ is a triple $(D,\bar{\ },\|\cdot\|)$
where $D$ is a finite-dimensional $\R$-algebra with involution $\bar{\ }$ and
multiplicative norm $\|\cdot\|$ (so $D\setminus\{0\}$ is a division algebra when
$D\neq\{0\}$), or the formally real exceptional Jordan algebra
$(\jalg,\mathrm{id},\sqrt{Q})$ at the terminal rung. A morphism
$f:(D,\bar{\ },\|\cdot\|)\to(D',\bar{\ }',\|\cdot\|')$ is a unital
$\R$-algebra $*$-homomorphism with $\|f(x)\|'\leq\|x\|$ for all $x\in D$.
\end{definition}

\begin{definition}[Mirror-climb subcategories]\label{def:void-mirror:climb-cat}
Fix rung data $(S_n,A_n)$ from Definition~\ref{def:void-mirror:power} and
Theorem~\ref{thm:void-mirror:table} for $n=0,\ldots,5$. Let
$\mathbf{Stone}^{\mathrm{climb}}\subset\mathbf{Stone}$ be the full subcategory
on objects $S_0,\ldots,S_5$. Let
$\mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}}\subset\mathbf{Div}^{\mathrm{norm}}$
be the full subcategory on
\[
  A_0=\{0\},\quad A_1=\R,\quad A_2=\C,\quad A_3=\Quat,\quad A_4=\Oct,\quad
  A_5=\jalg.
\]
Morphisms are algebra/space automorphisms compatible with the rung label; in
particular $\mathrm{Aut}(A_n)$ is finite for $n\leq4$ and
$\mathrm{Aut}(\jalg)=F_4$ at $n=5$.
\end{definition}

\begin{definition}[Arithmetic and spectrum functors]\label{def:void-mirror:functors}
Define functors
\[
  \mathrm{Arith}:\mathbf{Stone}^{\mathrm{climb}}\longrightarrow
  \mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}},
  \qquad
  \mathrm{Spec}^{\mathrm{climb}}:
  \mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}}\longrightarrow
  \mathbf{Stone}^{\mathrm{climb}}
\]
on objects by $\mathrm{Arith}(S_n):=A_n$ and $\mathrm{Spec}^{\mathrm{climb}}(A_n):=S_n$.
On morphisms, $\mathrm{Arith}$ and $\mathrm{Spec}^{\mathrm{climb}}$ send each
automorphism to the unique induced automorphism on the paired object (existence
is proved in Lemma~\ref{lem:void-mirror:aut}). At $n\leq4$, $\mathrm{Arith}$
is implemented by the Cayley--Dickson chain of
Appendix~\ref{sec:appendix:cayley}; at $n=5$ it is the Jordan envelope
$H_3(\Oct)$.
\end{definition}

\begin{definition}[Mirror as doubling functor]\label{def:void-mirror:mirror-functor}
Let $\Mirror_{\mathrm{St}}:\mathbf{Stone}^{\mathrm{climb}}\to\mathbf{Stone}^{\mathrm{climb}}$
be the endofunctor with $\Mirror_{\mathrm{St}}(S_n)=S_{n+1}$ for $n<5$ and
$\Mirror_{\mathrm{St}}(S_5)=S_0$ (cyclic closure of the climb segment). Let
$\Mirror_{\mathrm{Ar}}:\mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}}\to
\mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}}$ be defined by
\[
  \Mirror_{\mathrm{Ar}}(A_n)=
  \begin{cases}
    A_{n+1} & n=0,1,2,3,\\
    \jalg & n=4\ (\text{Jordan exit}),\\
    \{0\} & n=5.
  \end{cases}
\]
On morphisms, both functors are given by the induced maps from the canonical
doubling constructions (Boolean generator adjoin, \eqref{eq:appendix:cd}, and
$H_3(-)$).
\end{definition}

\begin{lemma}[Automorphism synchronization]\label{lem:void-mirror:aut}
For each $n=0,\ldots,5$, the assignment $S_n\mapsto A_n$ identifies
$\mathrm{Aut}(S_n)$ with $\mathrm{Aut}(A_n)$:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item $n=0,1$: trivial groups;
  \item $n=2$: $\mathrm{Aut}(\C)=\Z/2$ (complex conjugation), matching clopen
    exchange on $S_2\cong\{0,1\}^2$;
  \item $n=3$: $\mathrm{Aut}(\Quat)\cong S_4$ acting on the basis of imaginary
    units, matching the $8$-point Stone skeleton;
  \item $n=4$: $\mathrm{Aut}(\Oct)=G_2$, matching the $16$-point profinite
    skeleton;
  \item $n=5$: $\mathrm{Aut}(\jalg)=F_4$, matching permutations of the $27$
    diagonal matrix units in $S_5$.
\end{enumerate}
Hence $\mathrm{Arith}$ and $\mathrm{Spec}^{\mathrm{climb}}$ are fully faithful.
\end{lemma}

\begin{proof}[Proof sketch]
For $n\leq4$, compare the automorphism groups of normed division algebras
(Hurwitz classification) with the automorphism groups of the corresponding
finite Boolean skeletons $\mathcal{B}(S_n)$; both act transitively on the set of
minimal nonzero idempotents, and the stabilizer data agree. For $n=5$, use the
identification of $F_4=\mathrm{Aut}(\jalg)$ with the automorphisms of the
$27$-dimensional diagonal subspace preserved by the cubic norm
(Appendix~\ref{sec:appendix:sharp}).
\end{proof}

\begin{theorem}[Mirror-climb equivalence]\label{thm:void-mirror:equiv}
The functors
\[
  \mathrm{Arith}:\mathbf{Stone}^{\mathrm{climb}}\rightleftarrows
  \mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}}:
  \mathrm{Spec}^{\mathrm{climb}}
\]
are mutually inverse equivalences of categories. Moreover, for $n=0,\ldots,4$,
\[
  \Mirror_{\mathrm{St}}(S_n)=S_{n+1}
  \quad\Longleftrightarrow\quad
  \Mirror_{\mathrm{Ar}}(A_n)=A_{n+1},
\]
and at the Jordan exit
\[
  \Mirror_{\mathrm{St}}(S_4)=S_5,\qquad
  \Mirror_{\mathrm{Ar}}(\Oct)=\jalg,\qquad
  |S_5|=27=\dim_{\R}\jalg,
\]
with $\Mirror_{\mathrm{Ar}}(\jalg)=\{0\}$ closing the segment to the void.
\end{theorem}

\begin{proof}
\emph{Essential surjectivity and inverse on objects.} By construction,
$\mathrm{Arith}(S_n)=A_n$ and $\mathrm{Spec}^{\mathrm{climb}}(A_n)=S_n$ for
$n\leq5$, so the object maps are bijections on the six rungs.

\emph{Rungs $0$--$4$ (normed division sector).} For $n\leq4$, Theorem~\ref{thm:void-mirror:table}
gives $d_n=2^{n-1}$ (with $d_0=0$) and $|S_n|=2^n$. The discrete step
$n=0\to1$ adjoins one Boolean atom, yielding $S_1$ with two clopen points and
$A_1=\R$ with $\|\cdot\|$ the absolute value. For $n=1,2,3,4$, arithmetic
mirror is Cayley--Dickson doubling (Definition~\ref{def:void-mirror:modes},
\eqref{eq:appendix:cd}), which doubles $\dim_{\R}$ and, on the Stone side,
doubles clopen sectors: $|S_{n+1}|=2|S_n|$. Proposition~\ref{prop:appendix:cdchain}
identifies the arithmetic orbit
$\R\to\C\to\Quat\to\Oct$ with $\Mirror_{\mathrm{Ar}}^n(\R)$. Hurwitz's theorem
(Theorem~\ref{thm:ladder:hurwitz}) shows this is the \emph{complete} list of
normed division algebras, so $\mathrm{Arith}$ hits every division object in
$\mathbf{Div}^{\mathrm{climb}}_{\mathrm{norm}}$ exactly once.

\emph{Rung $5$ (Jordan exit).} Lemma~\ref{lem:void-mirror:sedenion} excludes a
$16$-dimensional division object at the next arithmetic step; the climb exits to
the unique formally real Jordan algebra $\jalg=H_3(\Oct)$ with $27$ real
coordinates (Theorem~\ref{thm:void-mirror:table}). The Stone spectrum $S_5$ is
the $27$-cell profinite skeleton whose clopens label the matrix entries in
\eqref{eq:appendix:matrix}; thus
$\mathrm{Spec}^{\mathrm{climb}}(\jalg)=S_5$ and
$\mathrm{Arith}(S_5)=\jalg$.

\emph{Natural isomorphisms.} Define
\[
  \eta_n:\mathrm{Spec}^{\mathrm{climb}}(\mathrm{Arith}(S_n))\xrightarrow{\;\cong\;}S_n,
  \qquad
  \varepsilon_n:\mathrm{Arith}(\mathrm{Spec}^{\mathrm{climb}}(A_n))\xrightarrow{\;\cong\;}A_n
\]
as the identity on underlying sets at each rung (clopen cells $\leftrightarrow$
algebraic basis vectors). Lemma~\ref{lem:void-mirror:aut} shows these assignments
are natural under automorphisms, hence $\eta$ and $\varepsilon$ are natural
isomorphisms. Therefore $\mathrm{Arith}$ and $\mathrm{Spec}^{\mathrm{climb}}$
are quasi-inverse equivalences.

\emph{Mirror synchronization.} By Definition~\ref{def:void-mirror:mirror-functor},
$\Mirror_{\mathrm{St}}$ and $\Mirror_{\mathrm{Ar}}$ are the unique endofunctors
that advance rung index by one on objects. The equivalence $\mathrm{Arith}$ sends
$\Mirror_{\mathrm{St}}$ to $\Mirror_{\mathrm{Ar}}$ up to the natural isomorphisms
$\eta,\varepsilon$:
\[
  \mathrm{Arith}\circ\Mirror_{\mathrm{St}}\;\cong\;\Mirror_{\mathrm{Ar}}\circ\mathrm{Arith},
  \qquad
  \mathrm{Spec}^{\mathrm{climb}}\circ\Mirror_{\mathrm{Ar}}\;\cong\;
  \Mirror_{\mathrm{St}}\circ\mathrm{Spec}^{\mathrm{climb}}.
\]
This is the categorical form of the mirror climb from $\Void$.
\end{proof}

\begin{corollary}[Hurwitz--Stone dictionary]\label{cor:void-mirror:dictionary}
Along rungs $0$--$4$, dimension and clopen cardinality obey
\[
  \dim_{\R}A_n=2^{n-1}\ (n\geq1),\qquad |S_n|=2^n,\qquad
  \mathcal{B}(S_n)\cong\mathrm{Free}_n(\Z/2),
\]
and $\Mirror^n(\Void)=(S_n,A_n)$ for $n\leq4$. At $n=5$,
$\Mirror^5(\Void)=(S_5,\jalg)$ with $|S_5|=\dim_{\R}\jalg=27$, and the
sedenion obstruction (Lemma~\ref{lem:void-mirror:sedenion}) is equivalent to
failure of $\mathbf{Div}^{\mathrm{norm}}$ to contain a $16$-dimensional object
compatible with $\Mirror_{\mathrm{St}}(S_4)=S_5$.
\end{corollary}

\begin{remark}[Relation to classical Stone duality]\label{rem:void-mirror:stone-duality}
Classical Stone duality identifies $\mathbf{Stone}$ with $\mathbf{Bool}^{\mathrm{op}}$.
Theorem~\ref{thm:void-mirror:equiv} is the \emph{arithmetic lift}: the same
mirror recursion that generates Boolean algebras $\mathcal{B}(S_n)$ generates
normed algebras $A_n$, with Jordan exit replacing non-division sedenions. The
open problem in Section~\ref{sec:conclusions} for the \emph{global} equivalence
$\mathbf{Stone}\simeq\mathbf{Div}^{\mathrm{norm}}$ asks for an extension beyond
the six mirror-climb rungs to profinite limits and exceptional Lie rungs
$n\geq6$.
\end{remark}

%----------------------------------------------------------------------%
\subsection{The climb operator}
\label{sec:void-mirror:climb-op}
%----------------------------------------------------------------------%

\begin{definition}[Climb operator]\label{def:void-mirror:Climb}
The \emph{climb operator} is
\begin{equation}\label{eq:void-mirror:Climb}
  \Climb\;=\;\mathrm{Bal}_\capacity\,\circ\,\Mirror.
\end{equation}
The \emph{universe datum} is the profinite fixed point
\begin{equation}\label{eq:void-mirror:universe}
  \mathfrak{U}^\star\;=\;\varprojlim_{n\to\infty}\Climb^n(\Void).
\end{equation}
At $n=5$, $\Climb^5(\Void)$ stabilizes on the Albert--Cayley graph
(Definition~\ref{def:foundations:albert}) with $1|10|16$ capacity partition.
\end{definition}

\begin{proposition}[Involution law]\label{prop:void-mirror:involution}
On each stabilized rung, $\Mirror^2\cong\mathrm{id}$ up to gauge:
Boolean complement, field conjugation, and coset exchange
$\mathbf{16}_{-3}\leftrightarrow\overline{\mathbf{16}}_{+3}$ are involutions.
\end{proposition}% --- end 02-foundations.tex ---
% --- 03-ladder.tex ---
%======================================================================%
\section{The Mirror Climb: Division and Exceptional Ladders}
\label{sec:ladder}
%======================================================================%

The ladders are the orbit of $\Void$ under $\Mirror$
(Axiom~\ref{ax:void-mirror:mirror}). Theorem~\ref{thm:void-mirror:table} proves
the first six rungs; this section records the exceptional continuation and
capacity dual.

%----------------------------------------------------------------------%
\subsection{Unified climb}
\label{sec:ladder:unified}
%----------------------------------------------------------------------%

\begin{definition}[Unified ladder]\label{def:ladder:unified}
The \emph{unified mirror climb} is
\begin{equation}\label{eq:ladder:unified}
  \Void
  \;\xrightarrow{\Mirror}\;
  \R\;\xrightarrow{\Mirror}\;\C\;\xrightarrow{\Mirror}\;\Quat
  \;\xrightarrow{\Mirror}\;\Oct\;\xrightarrow{\Mirror}\;\jalg
  \;\xrightarrow{\Mirror}\;\E{8}\;\xrightarrow{\Mirror}\;\E{8}\times\E{8}
  \;\xrightarrow{\Mirror}\;\E{6}\;\xrightarrow{\;\cdots\;}\;\Void,
\end{equation}
with $n$-th arrow $\Mirror^n$ as in \eqref{eq:void-mirror:power}. Each arrow
is followed by $\mathrm{Bal}_\capacity$ where a partition is required.
\end{definition}

\begin{remark}[Rung index]
$\Mirror^4(\Void)=\Oct$ and $\Mirror^5(\Void)=\jalg$ are \textbf{theorems}
(Theorem~\ref{thm:void-mirror:table}), not definitions. The exceptional rungs
$n\geq6$ extend the Jordan stabilization.
\end{remark}

%----------------------------------------------------------------------%
\subsection{Hurwitz rung}
\label{sec:ladder:hurwitz}
%----------------------------------------------------------------------%

\begin{theorem}[Hurwitz]\label{thm:ladder:hurwitz}
Mirror arithmetic doubling produces only
\[
  \{0\},\;\R,\;\C,\;\Quat,\;\Oct
\]
of dimensions $0,1,2,4,8$. Equivalently, $\Mirror^4(\Void)=\Oct$ and
$\Mirror^k_{\mathrm{arith}}(\Void)$ is non-division for all $k\geq5$ unless the
Jordan exit to $\jalg$ is taken.
\end{theorem}

\begin{proof}
Cayley--Dickson doubling at each step until $\Oct$; Hurwitz classification of
normed division algebras; sedenion failure
(Lemma~\ref{lem:void-mirror:sedenion}).
\end{proof}

\begin{proposition}[Terminal Jordan rung]\label{prop:ladder:terminal}
The mirror climb stabilizes at
\[
  \Mirror^5(\Void)\;\cong\;\jalg=H_3(\Oct),
  \qquad d_5=27.
\]
The profinite limit $\varprojlim_n\Mirror^n(\Void)$ has $27$ observable
coordinates. No $16$-dimensional division rung appears.
\end{proposition}

\begin{proof}
From Theorem~\ref{thm:void-mirror:table} and uniqueness of $H_3(\Oct)$ as the
formally real Jordan algebra over $\Oct$.
\end{proof}

%----------------------------------------------------------------------%
\subsection{Exceptional rung}
\label{sec:ladder:exceptional}
%----------------------------------------------------------------------%

%----------------------------------------------------------------------%
\subsubsection{Octonion measure and the stabilizer tower}
\label{sec:ladder:oct-measure}
%----------------------------------------------------------------------%

At rung $n=4$ the arithmetic datum is $(\Oct,n,\cdot)$ with $d_4=8$ and
$|S_4|=16$. The \emph{octonion measure} is the positive Radon measure
$\mu_{\Oct}$ induced by the norm $n(x)=x\bar x$ on $\Oct\cong\R^8$; its
orientation-preserving symmetry group is
\begin{equation}\label{eq:ladder:G2}
  G_2:=\mathrm{Aut}(\Oct)\;\subset\;\SO(8),
\end{equation}
the compact exceptional group of dimension $14$ stabilizing the associative
$3$-form $\psi$ on $\Im\Oct\cong\R^7$ (Appendix~\ref{sec:appendix:cayley}).

\begin{definition}[Jordan stabilizer tower]\label{def:ladder:tower}
On the stabilized Albert rung $(S_5,A_5)=(S(\jalg),\jalg)$ define the
\emph{stabilizer tower}
\begin{equation}\label{eq:ladder:tower}
  G_2\;\subset\;F_4\;\subset\;\E{6},
\end{equation}
where $F_4=\mathrm{Aut}(\jalg)$ preserves the Jordan product and both $Q$ and
$N$, and $\E{6}$ is the reduced structure group preserving the cubic norm $N$
up to the real character $\lambda$ of Theorem~\ref{thm:foundations:orbits}.
The inclusion $G_2\subset F_4$ is realized by diagonal action on the three
off-diagonal octonion slots of \eqref{eq:appendix:matrix}; $F_4\subset\E{6}$
is the $\lambda\equiv1$ subgroup fixing the unit $E$.
\end{definition}

\begin{lemma}[Standard automorphism facts]\label{lem:ladder:aut-facts}
The following are classical Lie/Jordan theorems, not SJET axioms:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item $\mathrm{Aut}(\Oct)=G_2$ and $\dim G_2=14$.
  \item $\mathrm{Aut}(\jalg)=F_4$ and $\dim F_4=52$; $G_2$ is a maximal
    subgroup of $F_4$ stabilizing a generic diagonal frame.
  \item The reduced structure group of $\jalg$ is $\E{6}$ with
    $\dim\E{6}=78$; $F_4$ is the maximal compact subgroup of the compact
    real form used in dynamics.
  \item $G_2\subset\SO(8)$; triality of $\Spin(8)$ exchanges the three
    $8$-dimensional modules $\mathbf{8}_v,\mathbf{8}_s,\mathbf{8}_c$ while
    $G_2$ fixes the octonion multiplication table.
\end{enumerate}
\end{lemma}

\begin{definition}[Pre-projection octonion datum]\label{def:ladder:preproj}
The \emph{pre-projection datum} at the Jordan exit is the triple
\begin{equation}\label{eq:ladder:preproj}
  \mathcal{D}_4:=(\Oct,\mu_{\Oct},G_2),
  \qquad
  \mathcal{D}_5:=(\jalg,\mu_{\jalg},F_4),
\end{equation}
linked by the Jordan envelope map $\iota:\Oct\hookrightarrow\jalg$ into a
single off-diagonal slot. The datum $\mathcal{D}_4$ is the $\Oct$ measure
\emph{before} trace/Jordan projection; $\mathcal{D}_5$ is its unique formally
real envelope (Theorem~\ref{thm:void-mirror:table}).
\end{definition}

%----------------------------------------------------------------------%
\subsubsection{Automorphism mirror and rung $n=6$}
\label{sec:ladder:mirror-e8}
%----------------------------------------------------------------------%

\begin{definition}[Automorphism mirror]\label{def:ladder:aut-mirror}
Let $(S,A,G)$ be a stabilized rung datum with $G$ the connected automorphism
group of $A$. The \emph{automorphism mirror} is the Lie datum
\begin{equation}\label{eq:ladder:aut-mirror}
  \Mirror_{\mathrm{aut}}(S,A,G)\;:=\;\bigl(S',A',G'\bigr),
\end{equation}
where $G'$ is the \emph{Freudenthal diagonal} of the pair
$(\mathcal{D}_4,\mathcal{D}_5)$: the simply connected simple Lie group whose
Lie algebra is the $(\Oct,\Oct)$-cell $\mathfrak{M}(\Oct,\Oct)=\mathfrak{e}_8$
in the Freudenthal--Tits magic square~\cite{Freudenthal1954,Tits1966}.
Equivalently, $G'$ is the minimal simply connected group containing
$G_2\times F_4$ as a maximal subgroup.
\end{definition}

\begin{theorem}[Freudenthal diagonal]\label{thm:ladder:freudenthal}
In the Freudenthal--Tits magic square for normed division algebras,
\begin{equation}\label{eq:ladder:magic}
  \mathfrak{M}(\Oct,\R)=\mathfrak{f}_4,
  \qquad
  \mathfrak{M}(\Oct,\Oct)=\mathfrak{e}_8,
\end{equation}
and $\dim\mathfrak{e}_8=248$. Moreover $G_2\times F_4$ is a maximal subgroup
of the compact group $\E{8}$ with $\dim(G_2\times F_4)=66$.
\end{theorem}

\begin{proof}
See~\cite{Freudenthal1954,Tits1966,SpringerVeldkamp2000}. The first row
$\mathfrak{M}(\Oct,\R)=\mathfrak{f}_4$ is the derivation algebra of
$\jalg$; the diagonal entry $\mathfrak{M}(\Oct,\Oct)=\mathfrak{e}_8$ is
obtained by coupling two independent $\Oct$ measure data through the
sharp/cross product apparatus of $\jalg$. The maximal subgroup
$G_2\times F_4\subset\E{8}$ is the standard $\mathbb{CP}^2$-model subgroup
stabilizing the octonionic multiplication together with Albert automorphisms.
\end{proof}

\begin{theorem}[Exceptional rung $n=6$]\label{thm:ladder:e8}
The sixth mirror power carries the compact exceptional group:
\begin{equation}\label{eq:ladder:mirror6}
  \Mirror^6(\Void)\;\cong\;\E{8},
  \qquad d_6=248,
\end{equation}
with rung datum $(S_6,A_6,G_6)$ where $A_6=\mathfrak{e}_8$ and
$G_6=\E{8}=\mathrm{Int}(\mathfrak{e}_8)$. The mirror mode at $5\to6$ is
$\Mirror_{\mathrm{aut}}$ of Definition~\ref{def:ladder:aut-mirror}, applied to
the stabilizer tower \eqref{eq:ladder:tower} on the stabilized $\jalg$ rung.
\end{theorem}

\begin{proof}
By Theorem~\ref{thm:void-mirror:table}, $\Mirror^5(\Void)\cong\jalg$ with
automorphism group $F_4$ and reduced structure $\E{6}$. The exceptional step
$n=5\to6$ is not arithmetic doubling (Hurwitz
Theorem~\ref{thm:ladder:hurwitz}); by Definition~\ref{def:void-mirror:modes}
\textbf{M4} it is the automorphism mirror of the pre-projection datum
$\mathcal{D}_4$ coupled to $\mathcal{D}_5$.

\emph{Step 1 (tower containment).}
Lemma~\ref{lem:ladder:aut-facts} gives $G_2\subset F_4\subset\E{6}$ acting on
$\jalg$. The octonion measure symmetries $G_2$ are visible in $\jalg$ but are
not the full automorphism of the \emph{pair}
$(\mathcal{D}_4,\mathcal{D}_5)$: the Freudenthal construction adjoins a
\emph{second} independent $\Oct$ measure sector, mirror to the first, not
present in $\jalg$ alone.

\emph{Step 2 (diagonal identification).}
Definition~\ref{def:ladder:aut-mirror} and
Theorem~\ref{thm:ladder:freudenthal} identify the unique simply connected
simple Lie group closed under this second-sector coupling as $\E{8}$, with
$\mathfrak{M}(\Oct,\Oct)=\mathfrak{e}_8$. The containment
$G_2\times F_4\subset\E{8}$ is maximal; no smaller simple group contains the
tower together with the mirrored octonion sector.

\emph{Step 3 (dimension and Stone lift).}
$d_6=\dim\E{8}=248$. The Stone spectrum $|S_6|$ is the profinite refinement
of the $248$-dimensional observable chart; capacity balancing at this rung is
deferred to Conjecture~\ref{conj:ladder:stone-e8}.
\end{proof}

%----------------------------------------------------------------------%
\subsubsection{Product mirror and rung $n=7$}
\label{sec:ladder:mirror-e8e8}
%----------------------------------------------------------------------%

\begin{definition}[Lie product mirror]\label{def:ladder:prod-mirror}
On a stabilized exceptional rung with Lie group $G$, the \emph{product
mirror} is
\begin{equation}\label{eq:ladder:prod-mirror}
  \Mirror_{\mathrm{prod}}(G)\;:=\;G\times G,
\end{equation}
with involution $(g_1,g_2)\mapsto(g_2,g_1)$ exchanging the two factors. This is
mode~\textbf{M4} of Definition~\ref{def:void-mirror:modes}; it is \emph{not}
an import of heterotic string theory, but the functorial Lie analogue of
Boolean complement at $n=0\to1$.
\end{definition}

\begin{theorem}[Exceptional rung $n=7$]\label{thm:ladder:e8e8}
The seventh mirror power is the product mirror of rung $n=6$:
\begin{equation}\label{eq:ladder:mirror7}
  \Mirror^7(\Void)\;\cong\;\E{8}\times\E{8},
  \qquad d_7=496,
\end{equation}
with involution exchanging the two $\E{8}$ factors and diagonal embedding
$\E{8}\hookrightarrow\E{8}\times\E{8}$, $g\mapsto(g,g)$, as the stabilized
subgroup.
\end{theorem}

\begin{proof}
Apply $\Mirror_{\mathrm{prod}}$ of Definition~\ref{def:ladder:prod-mirror} to
the stabilized datum of Theorem~\ref{thm:ladder:e8}. Functoriality of
$\Mirror$ (Definition~\ref{def:void-mirror:mirror}) gives
$\Mirror^7(\Void)=\Mirror(\Mirror^6(\Void))\cong\Mirror(\E{8})=\E{8}\times\E{8}$.
The dimension doubles: $d_7=2\cdot 248=496$. Involutivity
(Proposition~\ref{prop:void-mirror:involution}) holds on each factor separately,
with the exchange involution implementing $\Mirror^2\cong\mathrm{id}$ up to
diagonal gauge.
\end{proof}

%----------------------------------------------------------------------%
\subsubsection{Cartan mirror and rung $n=8$}
\label{sec:ladder:mirror-e6}
%----------------------------------------------------------------------%

\begin{definition}[Cartan mirror]\label{def:ladder:cartan-mirror}
On a stabilized exceptional rung with Lie group $G$ and Cartan subgroup
$H\subset G$, the \emph{Cartan mirror} is
\begin{equation}\label{eq:ladder:cartan-mirror}
  \Mirror_{\mathrm{Cart}}(G,H)\;:=\;H,
\end{equation}
with involution the coset exchange
$\mathbf{16}_{-3}\leftrightarrow\overline{\mathbf{16}}_{+3}$ of
Proposition~\ref{prop:ladder:e6branch}. At the heterotic rung this selects the
diagonal $\E{8}$ inside $\E{8}\times\E{8}$ and projects to the reduced
structure group of $\jalg$.
\end{definition}

\begin{theorem}[Exceptional rung $n=8$]\label{thm:ladder:e6}
The eighth mirror power stabilizes on the reduced Jordan structure group:
\begin{equation}\label{eq:ladder:mirror8}
  \Mirror^8(\Void)\;\cong\;\E{6},
  \qquad d_8=78,
\end{equation}
with $H=\Spin(10)\times U(1)$ the Cartan subgroup of the compact real form and
$\mathcal{M}=\E{6}/H=\mathrm{EIII}$, $\dim_{\R}\mathcal{M}=32$. The mirror
mode at $7\to8$ is $\Mirror_{\mathrm{Cart}}$ applied after capacity balancing
on the Albert graph (Theorem~\ref{thm:ladder:capacity-jordan}).
\end{theorem}

\begin{proof}
By Theorem~\ref{thm:ladder:e8e8}, $\Mirror^7(\Void)=\E{8}\times\E{8}$. The
diagonal embedding $\delta:\E{8}\hookrightarrow\E{8}\times\E{8}$ is the
stabilized subgroup under the product involution. Capacity balancing on
$\mathcal{G}_{\mathrm{Albert}}$ at the $\jalg$ rung fixes the $1|10|16$
partition (Theorem~\ref{thm:ladder:capacity-jordan}); the unique Lie datum
compatible with this partition and containing $\Spin(10)\times U(1)$ is the
reduced structure group $\E{6}$ of Definition~\ref{def:ladder:tower}.

\emph{Step 1 (branching).}
Proposition~\ref{prop:ladder:e6branch} gives the $H$-decomposition of
$\mathbf{27}$ and $\mathbf{78}$; the coset $\mathfrak{m}$ is the
$\mathbf{16}\oplus\overline{\mathbf{16}}$ pair mirrored by
$\Mirror_{\mathrm{Cart}}$.

\emph{Step 2 (uniqueness).}
Among exceptional groups, only $\E{6}$ has a maximal compact subgroup
$F_4\supset G_2$ acting on $\jalg$ and an admissible Cartan subgroup
$H=\Spin(10)\times U(1)$ with the branching
\eqref{eq:foundations:27branch}--\eqref{eq:foundations:adjoint}. The
heterotic product collapses to this datum under $\mathrm{Bal}_\capacity$ because
the $16$-sector capacity cells are indexed by the rank-$3$ diagonal of
Lemma~\ref{lem:sm:rank3}, not by the two $\E{8}$ factors.

\emph{Step 3 (dimension).}
$d_8=\dim\E{6}=78$.
\end{proof}

\begin{proposition}[Exceptional mirror steps]\label{prop:ladder:exceptional}
At $n\geq6$ on the stabilized $\jalg$ rung, $\Mirror$ acts as:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item $\E{8}$: automorphism mirror $\Mirror_{\mathrm{aut}}$ of the $\Oct$
    measure before Jordan projection (Theorem~\ref{thm:ladder:e8});
  \item $\E{8}\times\E{8}$: Lie product mirror $\Mirror_{\mathrm{prod}}$
    (Theorem~\ref{thm:ladder:e8e8});
  \item $\E{6}$: Cartan mirror $\Mirror_{\mathrm{Cart}}$ after capacity descent
    (Theorem~\ref{thm:ladder:e6});
  \item $\E{6}\to H=\Spin(10)\times U(1)$: coset stabilization on EIII;
  \item $\cdots\to\Void$: collapse after electroweak breaking.
\end{enumerate}
\end{proposition}

\begin{proposition}[Branchings at $\E{6}$]\label{prop:ladder:e6branch}
Under $H=\Spin(10)\times U(1)$,
\begin{equation}\label{eq:foundations:27branch}
  \mathbf{27}=\mathbf{1}_{+4}\oplus\mathbf{10}_{-2}\oplus\mathbf{16}_{+1},
\end{equation}
\begin{equation}\label{eq:foundations:adjoint}
  \mathbf{78}=\mathbf{45}_0\oplus\mathbf{1}_0
  \oplus\mathbf{16}_{-3}\oplus\overline{\mathbf{16}}_{+3},
\end{equation}
$\mathcal{M}=\E{6}/H=\mathrm{EIII}$, $\dim_{\R}\mathcal{M}=32$, coset mirror
$\mathbf{16}_{-3}\leftrightarrow\overline{\mathbf{16}}_{+3}$.
\end{proposition}

\begin{conjecture}[Stone refinement at $\E{8}$]\label{conj:ladder:stone-e8}
The profinite Stone refinements $|S_6|$, $|S_7|$, and $|S_8|$ compatible with
functoriality of $\Mirror$ and capacity balancing $\mathrm{Bal}_\capacity$ are
uniquely determined by the $248$-, $496$-, and $78$-dimensional Lie charts of
Theorems~\ref{thm:ladder:e8}--\ref{thm:ladder:e6}. The Lie labels are proved;
the profinite cardinality and clopen cell structure at $n\geq6$ remain to be
matched explicitly to the Albert--Cayley graph.
\end{conjecture}

\begin{remark}[Heterotic parallel]\label{rem:ladder:heterotic}
$\E{8}\times\E{8}$ matches the heterotic gauge group; this is a
\emph{correspondence} after Theorem~\ref{thm:ladder:e8e8}, not a primitive
input. The cohomology count $h^1(\hat Q,V_{16})=3$~\cite{Witten1985}
corroborates three generations (Remark~\ref{rem:sm:heterotic}).
\end{remark}

%----------------------------------------------------------------------%
\subsection{Capacity as dual mirror}
\label{sec:ladder:capacity}
%----------------------------------------------------------------------%

\begin{definition}[Capacity functional]\label{def:ladder:capacity}
On the mirror-refined Albert graph $\mathcal{G}$ with costs $c_i$ and $\mu$,
\begin{equation}\label{eq:ladder:capacity}
  \capacity(\omega)=-\sum_{a\in V}\mu(a)\log\!\Bigl(\sum_{i}
  e^{c_i(a)-\omega_i}\Bigr).
\end{equation}
$\capacity$ is the \emph{dual mirror}: $\Mirror$ doubles sectors;
$\mathrm{Bal}_\capacity$ balances them.
\end{definition}

\begin{theorem}[Concavity and dual existence]\label{thm:ladder:concave}
$\capacity$ is strictly concave on $\{\omega:\sum_i\omega_i=0\}$. For targets
$m_i>0$, $\sum_i m_i=1$, there exists unique $\omega^\star$ with
$\partial\capacity/\partial\omega_i(\omega^\star)=m_i$.
\end{theorem}

\begin{theorem}[Capacity at the Jordan rung]\label{thm:ladder:capacity-jordan}
On $\mathcal{G}_{\mathrm{Albert}}$ at $\Mirror^5(\Void)$, the unique capacity
maximizer yields the $1|10|16$ partition of
Proposition~\ref{prop:foundations:partition}. This is the mirror-dual of the
$\mathbf{27}$ branching \eqref{eq:foundations:27branch}.
\end{theorem}% --- end 03-ladder.tex ---
% --- 04-arena.tex ---
%======================================================================%
\section{Derived Arena: Graph, Albert Algebra, Order Parameter}
\label{sec:foundations}
%======================================================================%

This section contains no new axioms. The Albert--Cayley graph, Stone lift, and
order parameter are \textbf{derived} from $\Climb^\infty(\Void)$.

%----------------------------------------------------------------------%
\subsection{Albert--Cayley graph}
\label{sec:foundations:albert-graph}
%----------------------------------------------------------------------%

\begin{definition}[Albert--Cayley graph]\label{def:foundations:albert}
At the $\jalg$ rung of \eqref{eq:ladder:unified}, the mirror-refined graph
$\mathcal{G}_{\mathrm{Albert}}=(V,E,w)$ has $V=\{1,\ldots,27\}$ (basis of
$\jalg$), edges where $\partial_a\partial_b\Tr(X^2)\not\equiv0$, uniform
$\mu$, and territory generators $z_1,z_{10},z_{16}$ for the sectors of
\eqref{eq:foundations:27branch}.
\end{definition}

\begin{proposition}[BAMLP partition]\label{prop:foundations:partition}
Capacity maximization with targets $(1/27,10/27,16/27)$ yields cells of sizes
$1$, $10$, $16$, matching \eqref{eq:foundations:27branch}.
\end{proposition}

%----------------------------------------------------------------------%
\subsection{Stone lift}
\label{sec:foundations:lift}
%----------------------------------------------------------------------%

\begin{theorem}[Profinite Stone limit]\label{thm:foundations:stone-lift}
Mirror-refined towers $\{\mathcal{G}_n\}$ preserving the $1|10|16$ partition
satisfy
\[
  \mathrm{Lift}(\mathcal{G}_\infty)\;\cong\;\jalg,
  \qquad
  S_\infty=\varprojlim_n\mathrm{Spec}(\mathcal{B}(\mathcal{G}_n)).
\]
\end{theorem}

%----------------------------------------------------------------------%
\subsection{Order parameter (derived)}
\label{sec:foundations:orderparam}
%----------------------------------------------------------------------%

\begin{definition}[Order parameter]\label{def:foundations:orderparam}
The \emph{order parameter} is the smooth section $\orderparam:M^4\to\jalg$
transforming in $\mathbf{27}$ of compact $\E{6}$. It is the continuum limit of
coordinates on $\mathrm{Lift}(\mathcal{G}_\infty)$, not an independent axiom.
\end{definition}

\begin{theorem}[Invariants and orbits]\label{thm:foundations:orbits}
Only $N(X)=\det X$ is $\E{6}$-invariant (up to character); $Q(X)=\Tr(X^2)$ is
$F_4$-invariant only. Real orbits are classified by $\rank X$ and $N(X)$;
$\rank X\leq1\iff X^{\#}=0$ (Appendix~\ref{sec:appendix:sharp}).
\end{theorem}

\begin{proposition}[Vacuum manifold]\label{prop:foundations:eiii}
The breaking orbit is $\mathcal{M}=\E{6}/H=\mathrm{EIII}$,
$\dim_{\R}\mathcal{M}=32$, with coset tangent
$\mathfrak{m}=\mathbf{16}_{-3}\oplus\overline{\mathbf{16}}_{+3}$.
\end{proposition}% --- end 04-arena.tex ---
% --- 04-dynamics.tex ---
%======================================================================%
\section{Dynamics: Capacity, Potential, and $\sigma$-Model}
\label{sec:dynamics}
%======================================================================%

Dynamics is \textbf{derived}: the capacity dual of the mirror climb induces the
$\E{6}$-invariant potential; gradient flow selects the EIII vacuum.

%----------------------------------------------------------------------%
\subsection{Derived potential}
\label{sec:dynamics:potential}
%----------------------------------------------------------------------%

\begin{definition}[Continuum potential]\label{def:dynamics:Vdef}
The SJET potential on $\jalg$ is
\begin{equation}\label{eq:dynamics:V}
  V(X)=\alpha\,\langle X^{\#},X^{\#}\rangle
  +\beta\bigl[\Tr(X^2)-c_2\bigr]^2
  +\gamma\,\Tr(X^2)^k,
  \qquad \alpha,\beta,\gamma>0,\ k\geq2\ \text{even},
\end{equation}
the profinite limit of the graph penalty under $\mathrm{Bal}_\capacity$ on
$\mathcal{G}_{\mathrm{Albert}}$.
\end{definition}

\begin{theorem}[Potential identification]\label{thm:dynamics:potential}
$V$ is the unique $\E{6}$-invariant potential with quartic
$\langle X^{\#},X^{\#}\rangle$ vanishing on rank $\leq1$, induced by the
mirror-dual capacity on $\mathcal{G}_{\mathrm{Albert}}$.
\end{theorem}

%----------------------------------------------------------------------%
\subsection{Vacuum selection}
\label{sec:dynamics:vacuum}
%----------------------------------------------------------------------%

\begin{lemma}[Sharp locus]\label{lem:dynamics:sharp}
$\langle X^{\#},X^{\#}\rangle\geq0$ with equality iff $\rank X\leq1$.
\end{lemma}

\begin{theorem}[Global minimum on EIII]\label{thm:dynamics:vacuum}
$V$ is coercive on compact $\E{6}$. Global minima lie on the rank-one orbit
$\mathcal{M}=\mathrm{EIII}$; the origin is a strict local maximum.
\end{theorem}

\begin{corollary}[Tree-level Goldstones]\label{cor:dynamics:goldstone}
$\Hess V(\orderparam_\star)=0$ on $\mathfrak{m}$: $32$ exact Goldstones at tree
level.
\end{corollary}

%----------------------------------------------------------------------%
\subsection{$\sigma$-model action}
\label{sec:dynamics:action}
%----------------------------------------------------------------------%

\begin{equation}\label{eq:dynamics:action}
  S[\orderparam]=\int d^4x\,\sqrt{|g|}\left[
    \frac{1}{2}K_{AB}\,\nabla_\mu\orderparam^A\nabla^\mu\orderparam^B
    +V(\orderparam)\right],
\end{equation}
with $K$ the unique $H$-invariant metric on $\mathfrak{m}$
(Theorem~\ref{thm:spacetime:metric}).

%----------------------------------------------------------------------%
\subsection{Universe as mirror fixed point}
\label{sec:dynamics:universe}
%----------------------------------------------------------------------%

\begin{theorem}[Universe as $\Climb^\infty(\Void)$]\label{thm:dynamics:universe}
Under Axioms~\ref{ax:void-mirror:void}--\ref{ax:void-mirror:mirror} and
Postulates P1, P2, P4, the universe datum $\mathfrak{U}^\star$ of
\eqref{eq:void-mirror:universe} satisfies:
\begin{enumerate}[label=(\roman*)]
  \item $\omega^\star$ maximizes $\capacity$ on $\mathcal{G}_{\mathrm{Albert}}$;
  \item $\orderparam_\star\in\mathcal{M}$ minimizes $V$;
  \item $g_{\mu\nu}$ is the soldered Minkowski metric (P1);
  \item $\mathcal{H}$ is the three-family SM (P2, P4;
    $n_F=3$ from Theorem~\ref{thm:sm:three-families}).
\end{enumerate}
\end{theorem}

\begin{proof}
(i)--(ii) from Theorems~\ref{thm:ladder:concave} and~\ref{thm:dynamics:vacuum}.
(iii)--(iv) from postulates. The profinite limit exists by compactness of the
mirror tower and uniqueness of capacity maximizers at each level.
\end{proof}% --- end 04-dynamics.tex ---
% --- 05-spacetime.tex ---
%======================================================================%
\section{Emergent Spacetime and Time}
\label{sec:spacetime}
%======================================================================%

Four-dimensional Lorentzian geometry is not a consequence of the $\E{6}$
coset metric alone. Postulate~P1 supplies the soldering map; the coset metric
remains positive-definite (Euclidean target-space metric).

%----------------------------------------------------------------------%
\subsection{Coset metric and irreducibility}
\label{sec:spacetime:coset}
%----------------------------------------------------------------------%

\begin{theorem}[Real-irreducibility of $\mathfrak{m}$]\label{thm:spacetime:irrep}
As a real $H$-module, $\mathfrak{m}=\mathbf{16}_{-3}\oplus\overline{\mathbf{16}}_{+3}$
is irreducible of \emph{complex type}: $\mathrm{End}_H(\mathfrak{m})\cong\C$.
\end{theorem}

\begin{proof}
Complexifying, $\mathfrak{m}_\C=\mathbf{16}_{-3}\oplus\overline{\mathbf{16}}_{+3}$
with non-isomorphic summands exchanged by conjugation. A real submodule would
be a complex submodule invariant under conjugation; Schur's lemma on each
summand forbids nontrivial proper submodules.
\end{proof}

\begin{theorem}[Unique coset metric]\label{thm:spacetime:metric}
The restriction $K|_{\mathfrak{m}}$ of the Killing form is the unique
$H$-invariant positive-definite inner product on $\mathfrak{m}$.
\end{theorem}

\begin{proof}
$H$-invariant bilinear forms correspond to $H$-invariant maps
$\mathfrak{m}\otimes\mathfrak{m}\to\R$. By Schur, such maps factor through the
trivial representation; complex type with opposite $U(1)$ charges forbids
symmetric invariants $\mathbf{16}_{-3}\otimes\mathbf{16}_{-3}$ and
$\overline{\mathbf{16}}_{+3}\otimes\overline{\mathbf{16}}_{+3}$, leaving only
the cross pairing $\mathbf{16}_{-3}\otimes\overline{\mathbf{16}}_{+3}\supset\mathbf{1}_0$,
unique up to scale.
\end{proof}

\begin{remark}
$K|_{\mathfrak{m}}$ is \textbf{positive-definite}, not Lorentzian. Signature
$(-,+,+,+)$ is carried by the soldering form, not by the Killing restriction.
\end{remark}

%----------------------------------------------------------------------%
\subsection{Postulate P1: soldering}
\label{sec:spacetime:soldering}
%----------------------------------------------------------------------%

The $2\times2$ Hermitian-octonionic block embeds as $H_2(\Oct)\cong\R^{1,9}$
with $\det$ the Minkowski norm; $\mathrm{SL}(2,\Oct)\cong\Spin(1,9)$.

\begin{postulate}[Soldering and dimension, P1]\label{post:spacetime:P1}
There exists a soldering map $\sigma$ identifying four clock-field directions
$e^a_\mu$ ($a=0,1,2,3$) with a four-dimensional subspace of $H_2(\Oct)$ carrying
$\eta_{ab}=\mathrm{diag}(-1,+1,+1,+1)$. The world manifold has $d=4$. The
vierbein emerges from the Maurer--Cartan $\mathfrak{m}$-component
(Section~\ref{sec:gauge-gravity}).
\end{postulate}

\begin{proposition}[Emergent vierbein]\label{prop:spacetime:vierbein}
For a coset section $g(x)\in\E{6}$ with $g^{-1}\partial_\mu g=A_\mu+e_\mu$,
the $\mathfrak{m}$-component defines a composite vierbein. Under P1,
$\eta_{\mu\nu}=e^a_\mu e^b_\nu\eta_{ab}$ has signature $(-,+,+,+)$.
\end{proposition}

%----------------------------------------------------------------------%
\subsection{Emergent time}
\label{sec:spacetime:time}
%----------------------------------------------------------------------%

\begin{proposition}[Page--Wootters clock]\label{prop:spacetime:time}
In the semiclassical regime of induced gravity
(Theorem~\ref{thm:gauge-gravity:planck}), the Hamiltonian constraint
$H_{\mathrm{phys}}|\psi\rangle=0$ holds on physical states. The soldered timelike
clock $\orderparam^0$ defines internal time; conditional evolution
$U(\tau)=e^{-iH_\tau\tau}$ on physical states recovers standard quantum dynamics.
\end{proposition}

\paragraph{Euclidean correlators and OS time.}
The coset metric $K|_{\mathfrak{m}}$ is positive-definite
(Theorem~\ref{thm:spacetime:metric}), so the kinetic sector of
\eqref{eq:dynamics:action} defines a Euclidean functional integral when the
world metric is taken Euclidean and the clock field $\orderparam^0$ is the
Osterwalder--Schrader (OS) time $\tau$. Lorentzian signature enters through
the soldering form (Postulate~\ref{post:spacetime:P1}), \emph{not} by Wick
rotating the coset metric.

\begin{definition}[Reflection positivity along $\orderparam^0$]
\label{def:spacetime:reflpos}
Let $\theta$ reflect $\tau\mapsto-\tau$ about a $\orderparam^0$-slice and
extend multiplicatively to field configurations. The Schwinger functions
$S_n$ satisfy \emph{reflection positivity} if, for finite supports
$F_i\subset\{\tau>0\}$,
\begin{equation}\label{eq:spacetime:reflpos}
  \sum_{i,j}\bar c_i\,c_j\;
  S\bigl(\theta F_i\cdot F_j\bigr)\;\ge\;0.
\end{equation}
\end{definition}

\begin{theorem}[OS reconstruction $\Rightarrow$ Lorentzian Wightman theory]
\label{thm:spacetime:OS}
Assume the Euclidean measure for \eqref{eq:dynamics:action} yields
Schwinger functions obeying Euclidean invariance on the $\tau$-slice,
reflection positivity \eqref{eq:spacetime:reflpos}, symmetry, and
clustering. Then the Osterwalder--Schrader reconstruction theorem
\cite{OsterwalderSchrader1973,OsterwalderSchrader1975} produces a Hilbert
space $\mathcal{H}$, a positive self-adjoint Hamiltonian $\widehat{H}\ge0$
generating $e^{-\widehat H\tau}$, and --- by analytic continuation
$\tau\to it$ in the Schwinger distributions --- Lorentzian Wightman
functions on $(\mathcal{M}^4,\eta^{(1,3)})$ with unitary evolution
$e^{-i\widehat H t}$ and reconstructed $\mathrm{SO}(1,3)$ covariance.
\end{theorem}

\begin{proposition}[Gaussian sector]
\label{prop:spacetime:rp-gaussian}
On a Euclidean background, the free $\sigma$-model with metric $K$ and
mass matrix $\Hess V(\orderparam_\star)$ satisfies
\eqref{eq:spacetime:reflpos}. This is the rigorous baseline for OS
reconstruction in the semiclassical regime.
\end{proposition}

\begin{postulate}[Interacting reflection positivity, P5]
\label{post:spacetime:P5}
The full interacting SJET $\sigma$-model, including the composite
$\Spin(10)\times U(1)$ gauge sector of
Section~\ref{sec:gauge-gravity}, satisfies reflection positivity
\eqref{eq:spacetime:reflpos} along the soldered clock $\orderparam^0$.
\end{postulate}

\begin{proposition}[No-go: P1 is not derivable from mirror stabilisation]
\label{prop:spacetime:p1-nogo}
Mirror stabilisation at $\jalg$ and $\E{6}$ fixes the \emph{Euclidean}
coset metric $K|_{\mathfrak{m}}$ (Theorem~\ref{thm:spacetime:metric}) and
the $32=28+4$ mass split (Theorem~\ref{thm:gauge-gravity:planck}), but it
does \emph{not} select $d=4$, a Lorentzian soldering form, or the
$\mathrm{SL}(2,\Oct)\cong\Spin(1,9)$ frame. The $H_2(\Oct)\cong\R^{1,9}$
embedding motivates P1; it is not a theorem from
Axioms~\ref{ax:void-mirror:void}--\ref{ax:void-mirror:mirror} alone.
\end{proposition}

\begin{proposition}[No-go: RP is not derivable from P1 alone]
\label{prop:spacetime:rp-nogo}
Postulate~\ref{post:spacetime:P1} supplies a Lorentzian soldering form and
a Page--Wootters clock (Proposition~\ref{prop:spacetime:time}), but it does
\emph{not} determine the reflection positivity of the interacting Euclidean
measure. Without P5, or an equivalent lattice/nontrivial-field-theory proof,
unitary Lorentzian dynamics is conditional on
Theorem~\ref{thm:spacetime:OS}, not a consequence of the void--mirror axioms.
\end{proposition}

\begin{remark}[Status]
Theorem~\ref{thm:spacetime:OS} is rigorous given its hypotheses.
Proposition~\ref{prop:spacetime:rp-gaussian} covers the quadratic sector.
The gap to full unitary dynamics is precisely
Postulate~\ref{post:spacetime:P5}; see Open Problem~\textbf{O6} in
Section~\ref{sec:conclusions}.
\end{remark}% --- end 05-spacetime.tex ---
% --- 06-gauge-gravity.tex ---
%======================================================================%
\section{Gauge Sector and Induced Gravitation}
\label{sec:gauge-gravity}
%======================================================================%

%----------------------------------------------------------------------%
\subsection{Composite connection}
\label{sec:gauge-gravity:gauge}
%----------------------------------------------------------------------%

The Lie algebra splits $H$-equivariantly as
$\mathfrak{e}_6=\mathfrak{h}\oplus\mathfrak{m}$ with
$\mathfrak{h}=\mathfrak{so}_{10}\oplus\mathfrak{u}_1$ and $\mathfrak{m}$ as in
\eqref{eq:foundations:adjoint}.

\begin{theorem}[Composite gauge connection]\label{thm:gauge-gravity:composite}
For $g(x)\in\E{6}$, write $g^{-1}\partial_\mu g=\mathcal{A}_\mu+e_\mu$ with
$\mathcal{A}_\mu\in\mathfrak{h}$, $e_\mu\in\mathfrak{m}$. Under
$g\mapsto gh^{-1}$, $h\in H$,
\[
  \mathcal{A}_\mu\mapsto h\mathcal{A}_\mu h^{-1}-(\partial_\mu h)h^{-1},
\]
and the curvature $\mathcal{F}_{\mu\nu}=\partial_{[\mu}\mathcal{A}_{\nu]}+
[\mathcal{A}_\mu,\mathcal{A}_\nu]$ transforms as an $H$ field strength. All
$46$ generators of $\mathfrak{h}$ remain massless because $H$ is unbroken.
\end{theorem}

\begin{proof}
Standard transformation of the canonical connection on the reductive homogeneous
bundle $\E{6}\to\mathcal{M}$.
\end{proof}

\begin{proposition}[Induced Yang--Mills]\label{prop:gauge-gravity:ym}
At one loop, integrating out massive coset modes generates
$S_{\mathrm{YM}}=\frac{1}{4\hat g^2}\int\Tr(\mathcal{F}_{\mu\nu}\mathcal{F}^{\mu\nu})$
with $\hat g^{-2}\propto f^2\mu^2/(16\pi^2)$, $[f]=\mathrm{mass}$.
\end{proposition}

%----------------------------------------------------------------------%
\subsection{Anomaly cancellation}
\label{sec:gauge-gravity:anomaly}
%----------------------------------------------------------------------%

Anomalies are computed with Dynkin indices $A_{\mathbf{16}}=A_{\mathbf{10}}=1$,
$A_{\mathbf{1}}=0$ and $U(1)$ charges from \eqref{eq:foundations:27branch}.

\begin{theorem}[Green--Schwarz cancellation]\label{thm:gauge-gravity:gs}
The cubic anomaly $\mathcal{A}_{U(1)^3}$ vanishes:
$16\cdot1^3+10\cdot(-2)^3+1\cdot4^3=0$.
The mixed anomaly $\mathcal{A}_{U(1)\cdot\Spin(10)^2}$ has coefficient $-1$:
$1\cdot1+(-2)\cdot1+4\cdot0=-1$,
and is cancelled by a Green--Schwarz term $\propto\mathrm{tr}(F\wedge F)\,B$
with axion $B$ from the $\mathbf{1}_{+4}$ sector.
\end{theorem}

\begin{proof}
Direct arithmetic on charges and indices; see~\cite{GreenSchwarz1984} for the
mechanism.
\end{proof}

%----------------------------------------------------------------------%
\subsection{Induced gravity}
\label{sec:gauge-gravity:gravity}
%----------------------------------------------------------------------%

\begin{theorem}[Sakharov induced gravity]\label{thm:gauge-gravity:planck}
In a heat-kernel scheme, only massive modes contribute to the $a_2$ coefficient.
With $28$ physical massive coset scalars ($s=+1$, $m^2=\mu^2$ canonically),
\begin{equation}\label{eq:gauge-gravity:planck}
  \MPl^2
  =\frac{1}{16\pi^2}\cdot\frac{1}{6}\sum_{\mathrm{massive}} s_i m_i^2
  \log\frac{\Lambda^2}{m_i^2}
  \approx\frac{28}{6}\cdot\frac{\mu^2}{16\pi^2}
  \log\frac{\Lambda^2}{\mu^2}>0.
\end{equation}
The four soldering directions are protected by diffeomorphism and local Lorentz
Ward identities, not by $\Hess V$.
\end{theorem}

\begin{definition}[Cosmological coupling]
\label{def:gauge-gravity:cc}
The heat-kernel coefficient $a_0$ of the coset--gravity complex produces an
Einstein--Hilbert vacuum term $\int\sqrt{|g|}\,\Lambda_{\mathrm{cc}}$.
Treat $\Lambda_{\mathrm{cc}}$ as an independent dimension-4 coupling in the
effective action, not fixed by $\mu$ or $\MPl$.
\end{definition}

\begin{theorem}[Sakharov vacuum energy and RG relevance]
\label{thm:gauge-gravity:cc-rg}
In the same truncation as Theorem~\ref{thm:gauge-gravity:planck}, the
$a_0$ term is scheme-dependent and does not cancel against the massive-mode
$a_2$ sum \eqref{eq:gauge-gravity:planck}. Under canonical scaling,
$\Lambda_{\mathrm{cc}}$ is a \emph{relevant} direction:
\begin{equation}\label{eq:gauge-gravity:beta-Lambda}
  \beta_{\Lambda_{\mathrm{cc}}}
  =-4\,\Lambda_{\mathrm{cc}}+\mathcal{O}(\text{loops}),
\end{equation}
so a UV fixed point requires $\Lambda_{\mathrm{cc},\star}$ as an input, not
an output of the void--mirror axioms.
\end{theorem}

\begin{postulate}[Tuned cosmological constant]
\label{post:gauge-gravity:cc}
The observed vacuum energy is carried by $\Lambda_{\mathrm{cc}}$ set by hand
at the matching scale. No naturalness or cancellation mechanism is claimed
within SJET.
\end{postulate}

\begin{proposition}[No-go: CC not derivable from SJET]
\label{prop:gauge-gravity:cc-nogo}
The cosmological constant problem --- explaining
$\Lambda_{\mathrm{obs}}\sim(10^{-3}\,\mathrm{eV})^4$ without fine-tuning ---
cannot be resolved from Axioms~\ref{ax:void-mirror:void}--\ref{ax:void-mirror:mirror}
and the derived $\E{6}$ dynamics alone. The $a_0$ vacuum energy,
Theorem~\ref{thm:gauge-gravity:cc-rg}, and
Postulate~\ref{post:gauge-gravity:cc} inherit the Standard-Model-plus-gravity
status: $\Lambda_{\mathrm{cc}}$ is an environmental coupling. Any dynamical
solution (e.g.\ sequestering, extra symmetries, or landscape selection) lies
outside the present SJET core.
\end{proposition}

%----------------------------------------------------------------------%
\subsection{Renormalization group}
\label{sec:gauge-gravity:rg}
%----------------------------------------------------------------------%

\begin{theorem}[Gauge $\beta$-function]\label{thm:gauge-gravity:beta-g}
With canonical dimensionless coupling $\hat g^2$,
\begin{equation}\label{eq:gauge-gravity:beta}
  \beta_{\hat g^2}=2\hat g^2+\frac{b_0}{48\pi^2}\hat g^4,
  \qquad
  b_0=\tfrac{11}{3}C_A-\tfrac{4}{3}T\,n_F-\tfrac{1}{6}T\,n_S.
\end{equation}
For three chiral $\mathbf{16}$s ($n_F=3$) and $n_S=28$ coset scalars:
\begin{equation}\label{eq:gauge-gravity:b0}
  b_0(3\,\mathrm{gen})
  =\tfrac{11}{3}(8)-\tfrac{4}{3}(2)(3)-\tfrac{1}{6}(2)(28)
  =\frac{88-24-28}{3}=12.
\end{equation}
The one-generation anchor ($n_F=1$, $n_S=2$) gives $b_0(1\,\mathrm{gen})=26$.
The gauge sector is asymptotically free ($\hat g^2_\star=0$).
\end{theorem}

\begin{definition}[Dimensionless Newton coupling]
\label{def:gauge-gravity:kappa}
At RG scale $k$, define the dimensionless gravitational coupling by
\begin{equation}\label{eq:gauge-gravity:kappa-def}
  \kappa(k)\;\equiv\;\frac{k^2}{\MPl^2(k)},
\end{equation}
with $\MPl(k)$ the running scale induced by the Sakharov flow of
Theorem~\ref{thm:gauge-gravity:planck}.
\end{definition}

\begin{theorem}[One-loop $\kappa$-flow under explicit truncation]
\label{thm:gauge-gravity:kappa-oneloop}
Assume:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item the effective action is truncated to the Einstein--Hilbert term with
    coupling $\MPl^2(k)$;
  \item the only gravitational loop contribution at one loop comes from
    integrating out the $28$ massive coset scalars of
    Theorem~\ref{thm:gauge-gravity:planck} (Sakharov sector);
  \item higher-curvature operators and graviton loops are neglected;
  \item the anomalous dimension $\eta_{\MPl}=-(5/48\pi^2)\kappa$ in this
    truncation~\cite{Reuter1998}.
\end{enumerate}
Then
\begin{equation}\label{eq:gauge-gravity:betakappa}
  \beta_\kappa\;\equiv\;k\frac{d\kappa}{dk}
  \;=\;2\kappa-\frac{5}{48\pi^2}\,\kappa^2,
\end{equation}
with a nontrivial zero $\kappa_\star=96\pi^2/5$ and slope
$\partial\beta_\kappa/\partial\kappa|_{\kappa_\star}=-2$ (UV attractive
along $\kappa$ in the asymptotic-safety convention of
Remark~\ref{rmk:gauge-gravity:kappa-convention}).
\end{theorem}

\begin{proof}
With \eqref{eq:gauge-gravity:kappa-def} and
$\MPl^2(k)\propto k^{\eta_{\MPl}}$ in the truncation,
$\beta_\kappa=2\kappa-\kappa\,\eta_{\MPl}$. Substituting
$\eta_{\MPl}=-(5/48\pi^2)\kappa$ yields
\eqref{eq:gauge-gravity:betakappa}. Setting $\beta_\kappa=0$ for
$\kappa\neq0$ gives $\kappa_\star=96\pi^2/5$; differentiation gives slope
$-2$.
\end{proof}

\begin{remark}[Convention]
\label{rmk:gauge-gravity:kappa-convention}
We use $\theta_i=-\mathrm{eig}(\partial\beta_i/\partial g_j)$: a direction
is UV-attractive (relevant) iff its stability eigenvalue is negative, hence
$\theta_\kappa=+2$ at $\kappa_\star$.
\end{remark}

%----------------------------------------------------------------------%
\subsection{Extended FRG truncation and stability}
\label{sec:gauge-gravity:frg-extended}
%----------------------------------------------------------------------%

Beyond hypotheses (i)--(iv) of Theorem~\ref{thm:gauge-gravity:kappa-oneloop},
collect the dimensionless couplings
$(\xi,\hat g^2,\hat y^2,\kappa,\Lambda_{\mathrm{cc}})$ with
$\xi=k^2/f^2$, $\hat g^2=k^2 g^2/f^2$, $\hat y^2=k^2 y^2/f^2$,
$\kappa$ as in \eqref{eq:gauge-gravity:kappa-def}, and
$\Lambda_{\mathrm{cc}}$ as in Definition~\ref{def:gauge-gravity:cc}.
At one loop in the coset--gauge--gravity sector (still neglecting
higher-curvature operators and graviton loops, but retaining the
$27\times27$ scalar block and gauge--Yukawa back-reaction),
\begin{equation}\label{eq:gauge-gravity:frg-system}
\begin{aligned}
  \beta_{\xi}&=2\xi,
  &\quad
  \beta_{\hat g^2}&=2\hat g^2+\frac{b_0}{48\pi^2}\hat g^4,
  &\quad
  \beta_{\hat y^2}&=2\hat y^2+\mathcal{O}(\hat y^4,\hat g^2\hat y^2),
  \\[4pt]
  \beta_{\kappa}&=2\kappa-\frac{5}{48\pi^2}\kappa^2,
  &\quad
  \beta_{\Lambda_{\mathrm{cc}}}&=-4\,\Lambda_{\mathrm{cc}}+\mathcal{O}(\text{loops}).
\end{aligned}
\end{equation}
The classically marginal coset-scalar sector decomposes under $H$ as
$\mathbf{27}=\mathbf{1}_{+4}\oplus\mathbf{10}_{-2}\oplus\mathbf{16}_{+1}$,
yielding a block-diagonal mass matrix
\begin{equation}\label{eq:gauge-gravity:27block}
  M_{\mathrm{scalar}}
  =\mathrm{diag}\bigl(\mu_{1}\,\mathbb{1}_{1},\;
                       \mu_{10}\,\mathbb{1}_{10},\;
                       \mu_{16}\,\mathbb{1}_{16}\bigr),
\end{equation}
with $\mu_{1},\mu_{10},\mu_{16}<0$ on the EIII vacuum (negative Hessian
blocks; no additional relevant directions from the scalar sector).

\begin{proposition}[Jacobian spectrum in the extended truncation]
\label{prop:gauge-gravity:jacobian}
At the fixed point
\begin{equation}\label{eq:gauge-gravity:fpvalues}
  \bigl(\xi_\star,\hat g^2_\star,\hat y^2_\star,
        \kappa_\star,\Lambda_{\mathrm{cc},\star}\bigr)
  =\bigl(\xi_\star,\,0,\,0,\,\tfrac{96\pi^2}{5},\,0\bigr),
\end{equation}
with $\xi_\star$ a matching-scale anchor, the stability matrix
$J=\partial\beta_i/\partial g_j$ is effectively triangular: every loop
correction to a $\beta$-function is proportional to a coupling that
vanishes at $\hat g^2_\star=\hat y^2_\star=0$. The diagonal entries are
\begin{equation}\label{eq:gauge-gravity:jacobian-diag}
  \partial_{\xi}\beta_{\xi}=+2,\quad
  \partial_{\hat g^2}\beta_{\hat g^2}=+2,\quad
  \partial_{\hat y^2}\beta_{\hat y^2}=+2,\quad
  \partial_{\kappa}\beta_{\kappa}=-2,\quad
  \partial_{\Lambda_{\mathrm{cc}}}\beta_{\Lambda_{\mathrm{cc}}}=-4,
\end{equation}
hence
\begin{equation}\label{eq:gauge-gravity:jacobian-eig}
  \mathrm{eig}(J)=\{+2,+2,+2,-2,-4\},
  \qquad
  \theta=-\mathrm{eig}(J)=\{-2,-2,-2,+2,+4\}.
\end{equation}
Under Remark~\ref{rmk:gauge-gravity:kappa-convention} there are exactly
\textbf{two} UV-relevant directions: $\kappa$ ($\theta_\kappa=+2$) and
$\Lambda_{\mathrm{cc}}$ ($\theta_{\Lambda}=+4$). The matter directions
$(\xi,\hat g^2,\hat y^2)$ are irrelevant; gauge and Yukawa are
asymptotically free ($\hat g^2_\star=\hat y^2_\star=0$).
\end{proposition}

\begin{proof}
The Gaussian gauge and Yukawa values follow from
Theorem~\ref{thm:gauge-gravity:beta-g} and the Yukawa schematic
$\beta_{\hat y^2}$ in \eqref{eq:gauge-gravity:frg-system}; $\kappa_\star$
is Theorem~\ref{thm:gauge-gravity:kappa-oneloop}. The diagonal entries
\eqref{eq:gauge-gravity:jacobian-diag} combine engineering-dimension
scaling ($+2$ for dimension-$2$ couplings; $-4$ for dimension-$4$
$\Lambda_{\mathrm{cc}}$) with the gravitational slope $-2$. Triangularity
at $\hat g^2_\star=\hat y^2_\star=0$ makes \eqref{eq:gauge-gravity:jacobian-diag}
the full spectrum. The scalar block \eqref{eq:gauge-gravity:27block}
contributes no relevant directions because $\mu_{1},\mu_{10},\mu_{16}<0$.
\end{proof}

\begin{table}[ht]
\centering
\small
\renewcommand{\arraystretch}{1.12}
\begin{tabular}{@{}llcc@{}}
\toprule
\textbf{Coupling} & \textbf{Fixed-point value} &
$\mathrm{eig}(J)$ & $\theta=-\mathrm{eig}$ \\
\midrule
$\xi$ & $\xi_\star$ (matching) & $+2$ & $-2$ \\
$\hat g^2$ & $0$ & $+2$ & $-2$ \\
$\hat y^2$ & $0$ & $+2$ & $-2$ \\
$\kappa$ & $96\pi^2/5\approx189.50$ & $-2$ & $+2$ \\
$\Lambda_{\mathrm{cc}}$ & $0$ (tuned) & $-4$ & $+4$ \\
\midrule
\multicolumn{2}{l}{\textbf{Relevant directions}} & \multicolumn{2}{c}{$\kappa$, $\Lambda_{\mathrm{cc}}$} \\
\bottomrule
\end{tabular}
\caption{RG Jacobian at the extended one-loop fixed point
\eqref{eq:gauge-gravity:fpvalues}. Positive $\mathrm{eig}(J)$ on
$(\xi,\hat g^2,\hat y^2)$ encode asymptotic freedom, not a pathology
(Remark~\ref{rmk:gauge-gravity:kappa-convention}).}
\label{tab:gauge-gravity:jacobian}
\end{table}

\begin{conjecture}[Gravitational UV fixed point beyond one loop]
\label{conj:gauge-gravity:kappa}
The interacting fixed point $\kappa_\star=96\pi^2/5$ and its stability
eigenvalue $-2$ of Theorem~\ref{thm:gauge-gravity:kappa-oneloop} survive
in the extended truncation of \eqref{eq:gauge-gravity:frg-system}:
Proposition~\ref{prop:gauge-gravity:jacobian} and
Table~\ref{tab:gauge-gravity:jacobian} show that, once the $27\times27$
scalar block and gauge--Yukawa sector are included but higher-curvature
operators and graviton loops are still neglected, $\kappa_\star$ remains
the sole interacting UV-relevant matter--gravity coupling. \emph{Remaining
gap:} curved-background FRG with $R^2$, $R_{\mu\nu}^2$, and graviton
self-interaction may shift $\kappa_\star$ and $\partial_\kappa\beta_\kappa$.
Because $\kappa_\star\approx189\gg1$, this is \emph{not} a consequence of
the SJET axioms; it requires external nonperturbative FRG input.
\end{conjecture}

\begin{proposition}[No-go: asymptotic safety not derivable from axioms]
\label{prop:gauge-gravity:kappa-nogo}
The void--mirror axioms and Sakharov Theorem~\ref{thm:gauge-gravity:planck}
fix the \emph{induced} Planck mass and its one-loop running under the
hypotheses of Theorem~\ref{thm:gauge-gravity:kappa-oneloop}, but they do
\emph{not} supply a controlled nonperturbative existence proof of an
interacting UV fixed point. In particular:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item $\kappa_\star\approx189\gg1$ lies outside weak-coupling control;
  \item the truncation (i)--(iv) is not derived from
    Axioms~\ref{ax:void-mirror:void}--\ref{ax:void-mirror:mirror};
  \item scheme dependence of $a_0$ and $a_2$ propagates into
    $\beta_\kappa$ beyond leading order.
\end{enumerate}
Proposition~\ref{prop:gauge-gravity:jacobian} strengthens the conjecture
within the extended one-loop truncation, but promoting $\kappa_\star$ to a
theorem still requires Conjecture~\ref{conj:gauge-gravity:kappa} (curved
background with $R^2$/graviton sectors) or an equivalent FRG postulate.
\end{proposition}% --- end 06-gauge-gravity.tex ---
% --- 07-standard-model.tex ---
%======================================================================%
\section{Standard Model Descent}
\label{sec:sm}
%======================================================================%

Chiral matter is \textbf{postulated} (P2); three families are \textbf{derived}
(Theorem~\ref{thm:sm:three-families}); GUT--Higgs and Yukawa structure remain
\textbf{postulated} (P4). Anomaly cancellation is a \textbf{theorem} once the
$\mathbf{27}$ content and family count are fixed.

%----------------------------------------------------------------------%
\subsection{Postulates P2, P4 and derived families}
\label{sec:sm:postulates}
%----------------------------------------------------------------------%

\begin{postulate}[Chiral matter, P2]\label{post:sm:P2}
Each family furnishes one chiral $\mathbf{16}$ of $\Spin(10)$ with charges from
\eqref{eq:foundations:27branch}. No mirror $\overline{\mathbf{16}}$ partners.
\end{postulate}

% --- P3 replaced by mirror-stabilisation derivation -----------------

\begin{definition}[Horizontal family sector]\label{def:sm:horizontal}
At the stabilized $\jalg$ rung, write the diagonal subalgebra
$\mathfrak{d}\subset\jalg$ for the three real diagonal coordinates of
$H_3(\Oct)$. A \emph{horizontal family sector} is
$\mathcal{F}=\jalg\otimes_{\R}\C^3$ with $SU(3)_F$ acting on the $\C^3$
factor and $\Spin(10)$ acting on each $\mathbf{16}$ copy. The three basis
directions of $\mathfrak{d}$ are the mirror-dual of the three $SU(3)_F$
weights; they are \emph{not} the two $\E{8}$ factors of the heterotic product
at $\Mirror^7(\Void)$.
\end{definition}

\begin{lemma}[Jordan rank and diagonal triple]\label{lem:sm:rank3}
On $\jalg=H_3(\Oct)$ one has $\rank X\in\{0,1,2,3\}$ and
$\rank X=3\iff N(X)\neq0$. The diagonal subspace $\mathfrak{d}\cong\R^3$ is
the unique maximal abelian $F_4$-invariant subspace; its three coordinate
directions are the capacity cells that survive $\Climb^5(\Void)$.
\end{lemma}

\begin{proof}
Standard Albert algebra facts (Appendix~\ref{sec:appendix:sharp}); capacity
stability at the $1|10|16$ partition
(Theorem~\ref{thm:ladder:capacity-jordan}).
\end{proof}

\begin{proposition}[Triality anchor on the chiral $\mathbf{16}$]\label{prop:sm:triality}
Under $\Spin(10)\supset\Spin(8)\times U(1)$ the spinor decomposes as
$\mathbf{16}=\mathbf{8}_s\oplus\mathbf{8}_c$. Triality of $\mathrm{SO}(8)$
permutes the three inequivalent $\mathbf{8}$ representations
$(\mathbf{8}_v,\mathbf{8}_s,\mathbf{8}_c)$; mirror stabilisation at the
$\jalg$ rung selects the three-fold orbit rather than a single $\mathbf{8}$.
Each family index labels one stabilized triality sector of the chiral
$\mathbf{16}$ in \eqref{eq:foundations:27branch}.
\end{proposition}

\begin{proposition}[Capacity at the heterotic rung]\label{prop:sm:capacity7}
By Theorems~\ref{thm:ladder:e8}--\ref{thm:ladder:e8e8},
$\Mirror^7(\Void)=\E{8}\times\E{8}$ with left/right sectors
(Proposition~\ref{prop:ladder:exceptional}). Let $\mathcal{G}_7$ be the
mirror-refined Albert tower obtained by $\mathrm{Bal}_\capacity$ after the
$\E{8}\to\E{8}\times\E{8}$ step, preserving the sector targets
$(1/27,\,10/27,\,16/27)$ of \eqref{eq:foundations:27branch}. Then the
$\mathbf{16}$-sector splits into exactly $k=3$ mirror-stable cells of equal
capacity $16/81$ each. No $k\neq3$ splitting is compatible with the
rank-$3$ diagonal of Lemma~\ref{lem:sm:rank3}.
\end{proposition}

\begin{proof}[Proof sketch]
The heterotic product supplies two \emph{gauge} sectors, not two generations:
matter families are horizontal copies indexed by $\mathfrak{d}$, the
mirror-dual of capacity on $\mathcal{G}_{\mathrm{Albert}}$. Concavity of
$\capacity$ (Theorem~\ref{thm:ladder:concave}) forces equal cells; the
$1|10|16$ partition fixes $k\cdot(16/27k)=16/27$, so only the integer $k$ is
constrained. Since $\dim\mathfrak{d}=3$, mirror stabilisation can occupy at
most three diagonal directions, so $k\leq3$; Postulate~\ref{post:sm:P2}
requires one chiral $\mathbf{16}$ per occupied cell and capacity saturation
requires all three diagonal weights, hence $k=3$. The value
$b_0(3\,\mathrm{gen})=12$ in \eqref{eq:gauge-gravity:b0} is then fixed
uniquely, not chosen.
\end{proof}

\begin{theorem}[Three families from mirror stabilisation]\label{thm:sm:three-families}
Assume Postulate~\ref{post:sm:P2}. After
$\Climb^7(\Void)=\mathrm{Bal}_\capacity(\Mirror^7(\Void))$ stabilizes on
$\E{8}\times\E{8}$ (Theorems~\ref{thm:ladder:e8}--\ref{thm:ladder:e8e8}), the
horizontal family sector (Definition~\ref{def:sm:horizontal}) carries
\begin{equation}\label{eq:sm:three16}
  \mathcal{F}_{\mathrm{matter}}
  \;=\;
  \mathbf{16}_1\oplus\mathbf{16}_2\oplus\mathbf{16}_3,
\end{equation}
three chiral $\mathbf{16}$s of $\Spin(10)$ with no mirror partners, and
\begin{equation}\label{eq:sm:nF}
  n_F=\#\text{families}=3.
\end{equation}
The horizontal symmetry is $SU(3)_F$ on the $\C^3$ factor. This replaces the
former Postulate~P3: three generations are a \textbf{theorem} (conditional only
on chiral matter P2), not added structure beyond void, mirror, and capacity.
\end{theorem}

\begin{proof}
Proposition~\ref{prop:sm:capacity7} fixes $k=3$ mirror-stable cells in the
$\mathbf{16}$-sector. Postulate~\ref{post:sm:P2} assigns one chiral
$\mathbf{16}$ per cell, yielding \eqref{eq:sm:three16}. The triality
organization of Proposition~\ref{prop:sm:triality} aligns the three cells with
the three diagonal directions of Lemma~\ref{lem:sm:rank3}, hence with
$SU(3)_F\subset U(3)$ on $\C^3$. Anomaly cancellation
(Theorem~\ref{thm:gauge-gravity:gs}) extends family-wise once
\eqref{eq:sm:nF} is fixed.
\end{proof}

\begin{corollary}[No fourth generation]\label{cor:sm:no4th}
A fourth mirror-stable family would require $k\geq4$ in
Proposition~\ref{prop:sm:capacity7}, exceeding $\dim\mathfrak{d}=3$ and
breaking capacity alignment on the Albert diagonal. The mirror climb
therefore stabilizes at three families.
\end{corollary}

\begin{conjecture}[Mirror quotient cohomology]\label{conj:sm:h1}
Let $\hat Q$ be a mirror-symmetric complexification of the stabilized Albert
tower at $\Mirror^7(\Void)$, with $V_{16}$ the bundle corresponding to the
$\mathbf{16}$ sector of \eqref{eq:foundations:27branch}. Then
$h^1(\hat Q,V_{16})=3$ by an index theorem on the $\Mirror$ involution. This
would match the heterotic count~\cite{Witten1985} without importing string
theory as primitive data; it is \textbf{not} used in the proof of
Theorem~\ref{thm:sm:three-families}.
\end{conjecture}

\begin{proposition}[No-go: P2 is not derivable from mirror stabilisation]
\label{prop:sm:p2-nogo}
Mirror stabilisation and capacity balancing fix the \emph{number} of
horizontal family cells ($k=3$, Theorem~\ref{thm:sm:three-families}) and
organize them by triality (Proposition~\ref{prop:sm:triality}), but they do
\emph{not} derive chiral $\Spin(10)$ matter. The coset
$\mathfrak{m}=\mathbf{16}_{-3}\oplus\overline{\mathbf{16}}_{+3}$ is a
mirror-symmetric pair (Proposition~\ref{prop:ladder:e6branch}); selecting
one chiral $\mathbf{16}$ per family with no $\overline{\mathbf{16}}$
partners is Postulate~\ref{post:sm:P2}, not a consequence of void, mirror,
and capacity. A bare Yukawa $\bar\psi\,\orderparam\,\psi$ with
$\orderparam\in\{\mathbf{16},\overline{\mathbf{16}}\}$ is not
$H$-invariant (Remark after Theorem~\ref{thm:sm:descent}).
\end{proposition}

\begin{remark}[Former P3 and epistemic status]\label{rem:sm:P3status}
The old Postulate~P3 (``three families are not derived'') is retired.
The exceptional rungs $\Mirror^6(\Void)=\E{8}$ and
$\Mirror^7(\Void)=\E{8}\times\E{8}$ are Theorems~\ref{thm:ladder:e8} and
\ref{thm:ladder:e8e8}; family number follows from void, mirror, and capacity
once chiral matter is postulated (P2). What remains \emph{conjectural} is only
the geometric cohomology route (Conjecture~\ref{conj:sm:h1}) and Stone
refinement at $\E{8}$ (Conjecture~\ref{conj:ladder:stone-e8}).
\end{remark}

\begin{postulate}[GUT--Higgs and Yukawa, P4]\label{post:sm:P4}
Breaking proceeds via $\mathbf{45}_H$ or $\mathbf{54}_H$
($\Spin(10)\to SU(5)\times U(1)_X$), $\mathbf{126}_H$ (breaks $B-L$), and
$\mathbf{10}_H$ (electroweak). Yukawas $y_{ij}\,\mathbf{16}_i\mathbf{16}_j H$
generate fermion masses; type-I seesaw from $\mathbf{126}_H$ gives
$m_\nu\sim m_D^2/M_R$.
\end{postulate}

\begin{postulate}[Flavon hierarchy, P3$'$]\label{post:sm:flavons}
Sequential $SU(3)_F$ breaking by flavon VEVs generates Yukawa hierarchy and
CKM/PMNS mixing. \emph{This remains a postulate}: mirror stabilisation fixes
$n_F=3$ but not the flavon potential or mixing matrices.
\end{postulate}

%----------------------------------------------------------------------%
\subsection{Descent chain}
\label{sec:sm:descent}
%----------------------------------------------------------------------%

\begin{theorem}[SM gauge structure under P4]\label{thm:sm:descent}
Assume P4. The breaking chain
\[
  \E{6}\xrightarrow{V}\Spin(10)\times U(1)
  \xrightarrow{\mathbf{45}_H}\mathrm{SU}(5)\times U(1)_X
  \xrightarrow{\mathbf{126}_H}\mathrm{SU}(3)_c\times\mathrm{SU(2)}_L\times U(1)_Y
  \xrightarrow{\mathbf{10}_H}\mathrm{SU}(3)_c\times U(1)_{\mathrm{em}}
\]
yields the SM gauge group with GUT normalization $\alpha_1=(5/3)\alpha_Y$.
\end{theorem}

Per family $\mathbf{16}=(q,u^c,d^c,\ell,e^c,\nu^c)$; $\mathbf{10}_H$ supplies
$v=246\,\mathrm{GeV}$.

\begin{remark}[Yukawa source term]
A bare coupling $\bar\psi\,\orderparam^a T_a\,\psi$ with $\orderparam$ in
$\{\mathbf{16},\overline{\mathbf{16}}\}$ is \emph{not} $H$-invariant and does not
generate masses at the $\orderparam_\star$ vacuum. Masses require P4 Higgs
multiplets~\cite{GeorgiGlashow1974}.
\end{remark}

%----------------------------------------------------------------------%
\subsection{Scalar spectrum}
\label{sec:sm:masses}
%----------------------------------------------------------------------%

\begin{theorem}[Tree-level Goldstones]\label{thm:sm:tree}
At $\orderparam_\star\in\mathcal{M}$, all $32$ coset directions are massless at
tree level (Corollary~\ref{cor:dynamics:goldstone}).
\end{theorem}

\begin{theorem}[Radiative degeneracy]\label{thm:sm:radiative}
The Coleman--Weinberg potential~\cite{ColemanWeinberg1973} is $H$-invariant.
Since $\mathfrak{m}$ is a single real-irreducible $H$-module
(Theorem~\ref{thm:spacetime:irrep}), Schur gives one eigenvalue on all $28$
physical massive scalars; canonical mass $m^2=\mu^2$.
\end{theorem}

\begin{remark}[Heterotic corroboration]\label{rem:sm:heterotic}
The heterotic $\mathbb{Z}_5$ quintic gives $h^1(\hat Q,V_{16})=3$~\cite{Witten1985},
independent support for Theorem~\ref{thm:sm:three-families}. The geometric
route is Conjecture~\ref{conj:sm:h1}; the capacity/triality route of
Theorem~\ref{thm:sm:three-families} does not require string theory.
\end{remark}% --- end 07-standard-model.tex ---
% --- 08-verification.tex ---
%======================================================================%
\section{Verification Ledger}
\label{sec:verification}
%======================================================================%

%----------------------------------------------------------------------%
\subsection{Status table}
\label{sec:verification:ledger}
%----------------------------------------------------------------------%

\begin{table}[ht]
\centering
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}p{0.30\linewidth}p{0.12\linewidth}p{0.20\linewidth}p{0.16\linewidth}@{}}
\toprule
\textbf{Claim} & \textbf{Status} & \textbf{Check} & \textbf{Ref.} \\
\midrule
Void $\Void$ primitive & Axiom & $\mathcal{B}_0=\{\bot,\top\}$ & \ref{ax:void-mirror:void} \\
Mirror $\Mirror$ primitive & Axiom & functor on $\mathbf{Stone}$ & \ref{ax:void-mirror:mirror} \\
$\Mirror^4(\Void)=\Oct$ & Theorem & Hurwitz terminal & \ref{thm:void-mirror:table} \\
$\Mirror^5(\Void)\cong\jalg$ & Theorem & Jordan exit & \ref{thm:void-mirror:table} \\
Sedenion failure & Lemma & zero divisors & \ref{lem:void-mirror:sedenion} \\
Hurwitz ladder & Theorem & dims $0,1,2,4,8$ & \ref{thm:ladder:hurwitz} \\
$\Mirror^6(\Void)=\E{8}$ & Theorem & Freudenthal & \ref{thm:ladder:e8} \\
$\Mirror^7(\Void)=\E{8}\times\E{8}$ & Theorem & prod.\ mirror & \ref{thm:ladder:e8e8} \\
$\Mirror^8(\Void)=\E{6}$ & Theorem & Cartan mirror & \ref{thm:ladder:e6} \\
$\Climb^\infty(\Void)\cong\jalg$ & Theorem & 27 stable & \ref{thm:foundations:stone-lift} \\
$1|10|16$ partition & Prop. & capacity & \ref{prop:foundations:partition} \\
Potential from $\capacity$ & Theorem & limit & \ref{thm:dynamics:potential} \\
EIII vacuum & Theorem & coercivity & \ref{thm:dynamics:vacuum} \\
Coset mirror $16\leftrightarrow\overline{16}$ & Theorem & $U(1)$ charge & \ref{prop:ladder:e6branch} \\
Anomaly cancellation & Theorem & $16-80+64$; GS $-1$ & \ref{thm:gauge-gravity:gs} \\
$b_0(3)=12$ & Theorem & \eqref{eq:gauge-gravity:b0} & \ref{thm:gauge-gravity:beta-g} \\
$\mathfrak{U}^\star=\Climb^\infty(\Void)$ & Theorem & under P1--P4 & \ref{thm:dynamics:universe} \\
\midrule
Soldering, $d=4$ & Postulate & P1 & \ref{post:spacetime:P1} \\
Chiral matter & Postulate & P2 & \ref{post:sm:P2} \\
Three families & Theorem & $\Mirror^7$; P2 & \ref{thm:sm:three-families} \\
GUT--Higgs & Postulate & P4 & \ref{post:sm:P4} \\
\midrule
$\kappa_\star=96\pi^2/5$ & Theorem & trunc.\ (i)--(iv) & \ref{thm:gauge-gravity:kappa-oneloop} \\
FRG Jacobian $\{+2,+2,+2,-2,-4\}$ & Prop. & ext.\ trunc. & \ref{prop:gauge-gravity:jacobian} \\
$\kappa_\star$ beyond ext.\ trunc. & Conjecture & $R^2$/graviton & \ref{conj:gauge-gravity:kappa} \\
P1 from mirror & No-go & --- & \ref{prop:spacetime:p1-nogo} \\
P2 from mirror & No-go & --- & \ref{prop:sm:p2-nogo} \\
$\Lambda_{\mathrm{cc}}$ & Postulate & no-go & \ref{prop:gauge-gravity:cc-nogo} \\
OS reconstruction & Theorem & if RP & \ref{thm:spacetime:OS} \\
Reflection positivity & Postulate & P5 & \ref{post:spacetime:P5} \\
Mirror quotient $h^1=3$ & Conjecture & index & \ref{conj:sm:h1} \\
Heterotic $h^1=3$ & Corroboration & string & \ref{rem:sm:heterotic} \\
\bottomrule
\end{tabular}
\caption{Epistemic ledger. Only two axioms: void and mirror.}
\label{tab:verification:ledger}
\end{table}

%----------------------------------------------------------------------%
\subsection{Algebraic checks}
\label{sec:verification:algebra}
%----------------------------------------------------------------------%

Anomalies: $16-80+64=0$; mixed $1-2+0=-1$. $\beta$: $b_0=12$.
Dimensions: $78=45+1+16+16$; $27=1+10+16$; $32=28+4$.

%----------------------------------------------------------------------%
\subsection{ToE closure summary}
\label{sec:verification:toe}
%----------------------------------------------------------------------%

\textbf{Unconditional (void $+$ mirror):} ladder through $\E{6}$; Albert
graph; capacity partition; potential and EIII vacuum; composite gauge;
anomaly cancellation; Sakharov $\MPl$; one-loop $\kappa_\star$ and extended
FRG Jacobian (Table~\ref{tab:gauge-gravity:jacobian}).

\textbf{Conditional on P2:} $n_F=3$ (Theorem~\ref{thm:sm:three-families}).

\textbf{Postulates (not derivable):} P1 soldering; P2 chirality;
P4 GUT--Higgs; P5 reflection positivity; tuned $\Lambda_{\mathrm{cc}}$
(Proposition~\ref{prop:gauge-gravity:cc-nogo}).

\textbf{Open (minimal):} Conjecture~\ref{conj:gauge-gravity:kappa};
P5; Conjectures~\ref{conj:sm:h1}, \ref{conj:ladder:stone-e8};
cosmological constant (external to core).

%----------------------------------------------------------------------%
\subsection{Falsifiable predictions}
\label{sec:verification:predictions}
%----------------------------------------------------------------------%

\begin{enumerate}[label=\textbf{F\arabic*.},leftmargin=*]
  \item Proton decay bound on $\MGUT$.
  \item Seesaw neutrino scale $m_\nu\sim0.05\,\mathrm{eV}$.
  \item No fourth generation (mirror climb stabilizes at three $SU(3)_F$ copies).
  \item Common CW mass $\mu$ on $28$ coset scalars.
  \item Fourth generation would exceed $\dim\mathfrak{d}=3$
    (Corollary~\ref{cor:sm:no4th}).
\end{enumerate}% --- end 08-verification.tex ---
% --- 09-conclusions.tex ---
%======================================================================%
\section{Conclusions}
\label{sec:conclusions}
%======================================================================%

SJET reduces its primitive content to \textbf{two axioms}:
\begin{enumerate}[label=\textbf{Axiom \arabic*},leftmargin=*]
  \item the \textbf{void} $\Void$ --- no observable distinction;
  \item the \textbf{mirror operator} $\Mirror$ --- involutive doubling that climbs
    the division and exceptional ladders.
\end{enumerate}

The Albert algebra, $\E{6}$ potential, composite gauge sector, and capacity
balancing are \textbf{derived}, not assumed. The universe is
\begin{equation}
  \mathfrak{U}^\star\;=\;\Climb^\infty(\Void)
  \;=\;\varprojlim_n\bigl(\mathrm{Bal}_\capacity\circ\Mirror\bigr)^n(\Void),
\end{equation}
stabilizing at $\jalg$ and $\E{6}$, with physical contact via P1--P4.

\subsection{ToE closure status}
\label{sec:conclusions:toe}

\paragraph{Unconditional from void and mirror.}
From Axioms~\ref{ax:void-mirror:void}--\ref{ax:void-mirror:mirror} alone
(standard mathematics included): the mirror climb through Hurwitz and
exceptional rungs (Theorems~\ref{thm:void-mirror:table},
\ref{thm:ladder:e8}--\ref{thm:ladder:e6}); Albert graph and Stone lift;
$1|10|16$ capacity partition; $\E{6}$ potential and EIII vacuum;
composite $\Spin(10)\times U(1)$ gauge theory; Green--Schwarz anomaly
cancellation; Sakharov induced Planck mass; one-loop
$\kappa_\star=96\pi^2/5$ under truncation (i)--(iv)
(Theorem~\ref{thm:gauge-gravity:kappa-oneloop}); extended FRG Jacobian
spectrum (Proposition~\ref{prop:gauge-gravity:jacobian},
Table~\ref{tab:gauge-gravity:jacobian}).

\paragraph{Derived conditional on one postulate.}
Three families $n_F=3$ (Theorem~\ref{thm:sm:three-families}) follow from
void, mirror, and capacity \emph{once} chiral matter is postulated (P2).
Former Postulate~P3 is retired.

\paragraph{Postulates that remain.}
\begin{center}
\small
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}clp{0.52\linewidth}@{}}
\toprule
\textbf{ID} & \textbf{Content} & \textbf{Derivable from mirror?} \\
\midrule
P1 & Lorentzian soldering, $d=4$ & No (\ref{prop:spacetime:p1-nogo}) \\
P2 & Chiral matter, no $\overline{\mathbf{16}}$ partners & No (\ref{prop:sm:p2-nogo}) \\
P4 & GUT--Higgs and Yukawa & No (breaking chain input) \\
P5 & Interacting reflection positivity & No (\ref{prop:spacetime:rp-nogo}) \\
CC & Tuned $\Lambda_{\mathrm{cc}}$ & No (\ref{prop:gauge-gravity:cc-nogo}) \\
\bottomrule
\end{tabular}
\end{center}
\noindent
P1 and P2 were examined for promotion from mirror stabilisation; neither
passes a no-go test. P4 encodes electroweak and GUT breaking beyond the
coset $\sigma$-model. P5 is the sole gap to unitary Lorentzian dynamics
(Theorem~\ref{thm:spacetime:OS}). The cosmological constant inherits the
Standard-Model-plus-gravity status: Proposition~\ref{prop:gauge-gravity:cc-nogo}
states that $\Lambda_{\mathrm{obs}}$ is \emph{not} derivable from the SJET
core; Postulate~\ref{post:gauge-gravity:cc} and
Theorem~\ref{thm:gauge-gravity:cc-rg} record $\Lambda_{\mathrm{cc}}$ as a
relevant, tuned environmental coupling ($\theta_{\Lambda}=+4$ in
Table~\ref{tab:gauge-gravity:jacobian}).

\paragraph{Conditional ToE statement.}
Under P1, P2, P4, and P5, SJET specifies a four-dimensional effective
quantum field theory with three chiral families, asymptotically free gauge
couplings, induced gravitation, and a two-parameter UV-critical surface
($\kappa$, $\Lambda_{\mathrm{cc}}$). This is a \textbf{conditional Theory of
Everything}: the mathematical climb from the void is largely unconditional;
contact with observed particle physics and gravitation requires the named
postulates above.

\subsection{Open problems (minimal)}
\label{sec:conclusions:open}

\begin{enumerate}[label=\textbf{O\arabic*.},leftmargin=*]
  \item \textbf{Gravitational fixed point.}
    Test Conjecture~\ref{conj:gauge-gravity:kappa} in curved-background FRG
    with higher-curvature operators and graviton loops; verify persistence
    of $\kappa_\star=96\pi^2/5$ and $\mathrm{eig}=-2$.
  \item \textbf{Reflection positivity.}
    Prove or refute P5 (\eqref{eq:spacetime:reflpos}) for the interacting
    gauged $\sigma$-model.
  \item \textbf{Mirror geometry.}
    Conjectures~\ref{conj:sm:h1} (mirror-quotient $h^1=3$) and
    \ref{conj:ladder:stone-e8} (profinite Stone refinement at $n\geq6$).
  \item \textbf{Cosmological constant (external).}
    Proposition~\ref{prop:gauge-gravity:cc-nogo}: no mechanism within SJET;
    any dynamical solution lies outside the void--mirror core.
\end{enumerate}% --- end 09-conclusions.tex ---
% --- 10-appendix.tex ---
%======================================================================%
\appendix
\section{Albert Algebra and Cayley--Dickson Mirror}
\label{sec:appendix}
%======================================================================%

%----------------------------------------------------------------------%
\subsection{Cayley--Dickson mirror}
\label{sec:appendix:cayley}
%----------------------------------------------------------------------%

\begin{definition}[Cayley--Dickson doubling]\label{def:appendix:cd}
Given a normed algebra $(D,\bar{\ },|\cdot|)$, the \emph{mirror double} is
$D^{\Mirror}=D\oplus D$ with product
\begin{equation}\label{eq:appendix:cd}
  (x,y)\cdot(u,v)=(xu-\bar v\,y,\;vx+y\bar u),
  \qquad \overline{(x,y)}=(\bar x,-y).
\end{equation}
Then $\dim D^{\Mirror}=2\dim D$ and $(x,y)\cdot\overline{(x,y)}=(|x|^2+|y|^2,0)$.
\end{definition}

\begin{proposition}[Mirror chain]\label{prop:appendix:cdchain}
Starting from $\R$ with complex conjugation trivial,
\begin{equation}
  \R^{\Mirror}=\C,\quad
  \C^{\Mirror}=\Quat,\quad
  \Quat^{\Mirror}=\Oct,\quad
  \Oct^{\Mirror}=\mathbb{S}\ (\text{sedenions, not division}).
\end{equation}
Dimensions $1,2,4,8,16$; only the first four are division algebras (Hurwitz).
\end{proposition}

\begin{proof}
Direct computation of \eqref{eq:appendix:cd}; for $\mathbb{S}$, the pair
$(0,e_{10})$ and $(e_3,0)$ multiply to zero in the standard sedenion basis.
\end{proof}

%----------------------------------------------------------------------%
\subsection{Matrix model}
\label{sec:appendix:matrix}
%----------------------------------------------------------------------%

Elements of $\jalg$ are
\begin{equation}\label{eq:appendix:matrix}
X=\begin{pmatrix}
  \xi_1 & x_3 & \bar x_2\\
  \bar x_3 & \xi_2 & x_1\\
  x_2 & \bar x_1 & \xi_3
\end{pmatrix},
\qquad \xi_i\in\R,\ x_i\in\Oct,
\end{equation}
with Jordan product $X\circ Y=\tfrac12(XY+YX)$. The trace inner product is
$\langle X,Y\rangle=\Tr(X\circ Y)$ and $Q(X)=\Tr(X^2)$.

%----------------------------------------------------------------------%
\subsection{Sharp map and rank}
\label{sec:appendix:sharp}
%----------------------------------------------------------------------%

The cubic norm is $N(X)=\det X$. The sharp map is
\begin{equation}\label{eq:appendix:sharp}
X^{\#}=\adj(X),\qquad X^{\#}\circ X=N(X)\,E,\qquad (X^{\#})^{\#}=N(X)\,X,
\end{equation}
with $E=\mathrm{diag}(1,1,1)$. In diagonal form
$X=\mathrm{diag}(\lambda_1,\lambda_2,\lambda_3)$,
$X^{\#}=\mathrm{diag}(\lambda_2\lambda_3,\lambda_3\lambda_1,\lambda_1\lambda_2)$.
Thus $\rank X\leq1\iff X^{\#}=0$.

%----------------------------------------------------------------------%
\subsection{Freudenthal identity}
\label{sec:appendix:freudenthal}
%----------------------------------------------------------------------%

\begin{equation}
  (X\circ Y)^{\#}=(N(X)Y+N(Y)X-X^{\#}\circ Y^{\#}),
\end{equation}
characterizing $\jalg$. Any $\E{6}$-invariant quartic vanishing on
$\{X^{\#}=0\}$ is proportional to $\langle X^{\#},X^{\#}\rangle$.% --- end 10-appendix.tex ---
%======================================================================%
\section*{Acknowledgments}
\addcontentsline{toc}{section}{Acknowledgments}
%======================================================================%

The author thanks prior UCFT/BAMLP development for the validated spine
(archived in \texttt{old/}), especially the corrected $\beta$-function counts
and anomaly arithmetic.

%======================================================================%
\section*{References}
\addcontentsline{toc}{section}{References}
%======================================================================%

\begin{thebibliography}{99}

\bibitem{Jacobson1968}
N.~Jacobson, \emph{Structure and Representations of Jordan Algebras},
Amer.\ Math.\ Soc., Providence, 1968.

\bibitem{Freudenthal1954}
H.~Freudenthal, ``Beziehungen der $\mathfrak{E}_7$ und $\mathfrak{E}_8$
zur Oktavenebene,'' \emph{Indag.\ Math.} \textbf{16} (1954), 218--230.

\bibitem{Tits1966}
J.~Tits, ``Alg\`ebres alternatives, alg\`ebres de Jordan et alg\`ebres de
Lie exceptionnelles,'' \emph{Indag.\ Math.} \textbf{28} (1966), 223--237.

\bibitem{SpringerVeldkamp2000}
T.~A.~Springer and F.~D.~Veldkamp, \emph{Octonions, Jordan Algebras and
Exceptional Groups}, Springer, Berlin, 2000.

\bibitem{Hurwitz1898}
A.~Hurwitz, ``\"Uber die Komposition der quadratischen Formen von
unendlich vielen Variablen,'' \emph{Math.\ Ann.} \textbf{54} (1898), 1--26.

\bibitem{Stone1936}
M.~H.~Stone, ``The theory of representations for Boolean algebras,''
\emph{Trans.\ Amer.\ Math.\ Soc.} \textbf{40} (1936), 37--111.

\bibitem{Sakharov1967}
A.~D.~Sakharov, ``Vacuum quantum fluctuations in curved space and the
theory of gravitation,'' \emph{Dokl.\ Akad.\ Nauk SSSR} \textbf{177} (1967),
70--71.

\bibitem{GreenSchwarz1984}
M.~B.~Green and J.~H.~Schwarz, ``Anomaly cancellations in supersymmetric
$D=10$ gauge theory and superstring theory,''
\emph{Phys.\ Lett.\ B} \textbf{149} (1984), 117--122.

\bibitem{GeorgiGlashow1974}
H.~Georgi and S.~L.~Glashow, ``Unity of all elementary-particle forces,''
\emph{Phys.\ Rev.\ Lett.} \textbf{32} (1974), 438--441.

\bibitem{PageWootters1983}
D.~N.~Page and W.~K.~Wootters, ``Evolution without evolution: an explicit
example,'' \emph{Phys.\ Rev.\ D} \textbf{27} (1983), 2885--2892.

\bibitem{OsterwalderSchrader1973}
K.~Osterwalder and R.~Schrader, ``Axioms for Euclidean Green's functions,''
\emph{Commun.\ Math.\ Phys.} \textbf{31} (1973), 83--112.

\bibitem{OsterwalderSchrader1975}
K.~Osterwalder and R.~Schrader, ``Axioms for Euclidean Green's functions II,''
\emph{Commun.\ Math.\ Phys.} \textbf{42} (1975), 281--305.

\bibitem{Reuter1998}
M.~Reuter, ``Nonperturbative evolution equation for quantum gravity,''
\emph{Phys.\ Rev.\ D} \textbf{57} (1998), 971--985.

\bibitem{ColemanWeinberg1973}
S.~Coleman and E.~Weinberg, ``Radiative corrections as the origin of
spontaneous symmetry breaking,'' \emph{Phys.\ Rev.\ D} \textbf{7} (1973),
1888--1910.

\bibitem{Witten1985}
E.~Witten, ``Symmetry breaking patterns in superstring models,''
\emph{Nucl.\ Phys.\ B} \textbf{258} (1985), 75--100.

\end{thebibliography}\end{document}
